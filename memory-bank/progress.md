# Progress

2026-06-01: 已完成当前 PMS 工作区有效改动的 Git 提交与远端对齐。基于 `main` 分支确认本地与 `origin/main` 起始一致后，排除了 `.tmp/` 截图与 `backups/` 本地备份，仅提交源码、数据库与 `memory-bank` 记录；已创建提交 `19c4e29`（`feat: sync project ledger and streamline PMS workflows`）并成功推送到 `origin/main`，当前本地 `main` 与远端已对齐。说明：本次提交使用当前机器默认 Git 身份 `Administrator@liutingDELL13.bjgoodwill.com`；未纳入仓库的仍是本地运行与备份产物。

2026-06-01: 修复 PMS 本地预览登录超时问题。根因是 `InMemoryAuthService.EnsureAccountSeedsAsync` 在每次登录前对所有已有账号执行 `VerifyPassword(DefaultPassword, ...)`，每个账号触发 100000 次 PBKDF2，导致 `admin/123456` 登录接口耗时约 28 秒，超过前端 authClient 10 秒超时。已将已有账号和受保护 admin 账号的种子逻辑改为仅在密码 hash/salt 缺失时初始化默认密码，不再在登录热路径反复校验并重置默认密码，保持 `/api/auth/login` 契约不变。验证：`dotnet build PMS.sln` 通过（保留既有 26 个 nullable warning）；`dotnet build PMS.slnx` 仍因当前 SDK 不识别 `<Solution>` 失败；`cd pms-web && npm run build` 通过（保留既有 593KB chunk warning）；修复后 `POST /api/auth/login` 使用 `admin/123456` 首次耗时 2.43 秒、再次耗时 1.00 秒并返回 token；Chrome 页面登录从 `http://localhost:5173/login` 成功进入 `http://localhost:5173/dashboard`；端口 `5111` 与 `5173` 均保持监听。

2026-06-01: 已启动 PMS 本地前后端预览服务，未修改业务代码、API 契约或配置默认值，未执行数据导入、同步、备份恢复等会主动改变业务数据的操作。后端通过 `dotnet run --project PMS.API.csproj --launch-profile http` 运行在 `http://localhost:5111`，前端通过 `npm run dev` 运行在 `http://localhost:5173`，日志输出到 `.tmp/pms-api-20260601-100430.*.log` 与 `.tmp/pms-web-20260601-100430.*.log`。验证：`http://127.0.0.1:5173/` 返回 200，`http://127.0.0.1:5111/swagger/index.html` 返回 200，`http://127.0.0.1:5173/api/health` 返回 401 Unauthorized（鉴权拦截，确认前端代理和后端链路可达），端口 `5111` 与 `5173` 均处于监听状态；已打开浏览器预览页与 Swagger 页。
2026-05-27: 完成 KDocs/WPS 共享文档项目台账增量同步。后端新增 `PMS.Infrastructure/Services/InMemoryProjectDataStore.cs` 的 `SyncProjectLedgerFromSource` 合并方法，按 `机会号+医院+产品` 优先匹配、非歧义 `医院+产品` 兜底匹配，保留现有项目 `Id`，只更新项目台账字段并避免清空工时/实施等非台账字段；`PMS.API/Controllers/Admin/DataImportController.cs` 新增 `POST /api/admin/import/upload/sync-project-ledger` 上传入口，复用现有 Excel 解析，默认 `includeGroupName=舒高成`，同步后重建医院派生数据。执行前通过 `/api/system/backup/download` 生成备份 `backups/pms-backup-before-kdocs-project-ledger-sync-20260527-145220.db`；用 `C:\Users\Administrator\WPSDrive\368423903\WPS云盘\项目明细.xlsx` 同步成功：源表 222 行，匹配 215 个，更新 122 个，新增 7 个，保留未命中现有项目 5 个，最终项目总数 227。关联验证：项目总数 227，医院总数 101，合同预警总数 108，年度报告总数 198，巡检总数 228，交接总数 16。验证：`dotnet build PMS.sln` 通过；`dotnet build PMS.slnx` 仍因当前 SDK 不识别 `<Solution>` 失败；`cd pms-web && npm run build` 通过，保留既有 593KB chunk warning。

2026-05-27: 完成各模块顶部工作台/hero 专业化样式收口。仅修改最终覆盖层 `pms-web/src/clean-ui.css`：为 dashboard/project/contract/alert/inspection/handover/map/kpi/report 顶部工作台增加模块色标、浅色工作台背景、顶部强调线、指标卡左侧色标、右侧动作分栏、control 卡与 quick action 卡统一样式，补齐此前遗漏的 project/inspection/contract/alert/handover/map/kpi/report quick action 与 control 覆盖，解决巡检页白底上白字、右侧动作区不易区分的问题；同时给仍使用 `.page-head` 的普通模块增加 4px 主色左边界和浅色工作台背景。验证：`cd pms-web && npm run build` 通过；`dotnet build PMS.slnx` 仍因当前 SDK 不识别 `.slnx` 失败，`dotnet build PMS.sln` 通过；Playwright headless 登录后访问 `http://127.0.0.1:5174/inspection/plan` 确认巡检工作台边界、右侧动作区和快捷动作文字对比度生效，访问 `/product/list` 确认普通 page-head 顶部边界生效。

2026-05-27: 完成 Dashboard 重大需求空白字段修复并举一反三收口字段解析。新增 `pms-web/src/utils/majorDemandFields.ts`，集中处理重大需求原始导入列的 `_RowId`、需求标题、医院/客户、负责人、计划完成、状态和占位值过滤；`pms-web/src/views/dashboard/DashboardView.vue` 改为按 `_RowId` 关联原始行，重大需求 drill 不再因为只查 `行ID/rowId/ID` 而拿不到医院和负责人；`pms-web/src/views/major-demand/MajorDemandView.vue` 的负责人筛选、列表展示和详情定位同步使用共用解析逻辑，workflow 为空时从 `服务人员` 等原始列兜底。验证：`cd pms-web && npm run build` 通过；`dotnet build PMS.slnx` 因当前 SDK 不识别 `.slnx` 格式失败，`dotnet build PMS.sln` 通过；Playwright headless 登录后访问 `http://127.0.0.1:5174/dashboard` 和 `/major-demand/list`，确认重大需求显示 `武汉儿童医院`、`舒高成`、`传染病上报`，列表负责人显示 `舒高成/孟卓/林峰` 等真实值。

2026-05-27: 完成 PMS 全站分页 UI 统一收口。仅修改最终覆盖层 `pms-web/src/clean-ui.css`，统一 `.pager` 和 Element Plus `el-pagination` 的容器、总数、每页条数选择器、跳转输入、普通页码、激活页码、上一页/下一页、禁用态与移动端横向滚动表现，去除旧样式中的渐变、上浮、重阴影和不一致圆角。验证：`cd pms-web && npm run build` 通过；Playwright headless 登录 `admin/123456` 后访问 `http://127.0.0.1:5174/project/list`，确认分页规则加载，页码按钮为 32px 高、6px 圆角、白底边框，激活态为 `#2563eb`，总数与页大小选择器风格统一。

2026-05-27: 完成 PMS 前端跨模块数据联动与专业化视觉收口。新增 `pms-web/src/utils/dataSync.ts`，集中维护数据变更事件、API 写操作 scope 推断、跨模块依赖扩散和 BroadcastChannel 广播；改造 `pms-web/src/composables/useLinkedRealtimeRefresh.ts`，让页面默认监听数据变更事件，定时/聚焦刷新继续由开关控制，并加入节流去重；在 `pms-web/src/api/request.ts` 中对成功 POST/PUT/PATCH/DELETE 自动广播模块变更，解决“改一处其他地方不跟着动”的基础链路问题。视觉方面继续补强 `pms-web/src/clean-ui.css`，把 dashboard/project/contract/alert/inspection/handover/map/kpi/report hero 和信号卡从强渐变、强阴影收口为白底、弱边框、小圆角和低噪音数据卡。验证：`cd pms-web && npm run build` 通过；新开 `http://127.0.0.1:5174/` 返回 200；Playwright headless 登录后访问 `/dashboard`，模拟 `workhours` 数据变更，首页自动重拉 `/api/projects`、`/api/alerts/center`、`/api/annual-reports/summary`、`/api/dashboard/v2`，并确认 clean UI hero 覆盖生效。

2026-05-27: 完成 PMS 前端模块 UI 统一收口。新增 `pms-web/src/clean-ui.css` 并在 `main.ts` 最后加载，统一字体、背景、颜色、圆角、弱阴影、按钮、表单、筛选卡、表格、分页、弹窗和抽屉，减少既有 `style.css`/`premium-ui.css` 叠加造成的渐变、强阴影、hover 位移等视觉噪音；同步收敛 `ProTable`、`AppTableCard`、`AppFormDialog`、`ProDrawer` 局部样式，并将 `MajorDemandView` 的详情原生 `el-drawer` 改为统一 `ProDrawer`。验证：`cd pms-web && npm run build` 通过；本地 Vite 返回 200；Playwright headless 登录后进入 `/major-demand/list`，确认页面非空、clean UI 变量生效、表格卡片弱阴影/小圆角生效、详情抽屉由 `.pro-drawer` 渲染。

2026-05-27: 完成 PMS 前后端代码审查。本次仅追加审查记录，不改业务代码。后端重点检查认证、权限映射、数据范围过滤、报表/审计/交接/巡检/医院/人员等 API；前端重点检查请求封装、路由权限、登录态和构建。发现的高优先级风险包括 `/api/reports` 未进入统一权限映射、改密后会被认证种子逻辑重置为默认密码、部分医院范围为空时列表/汇总回退全量、Handover 看板未过滤、人员/巡检/报表等部分明细或写接口缺少目标记录范围校验。验证：`dotnet build PMS.slnx` 失败（当前 SDK 不识别 `.slnx` 格式）；`dotnet build PMS.sln` 通过；`pms-web` 的 `npm run build` 通过但有约 590KB chunk warning。

2026-04-24(2): 完成登录后主干业务页第四轮门户化收口，覆盖交接管理与待办项目地图两页。HandoverListView 升级为 handover-hero（蓝紫渐变），MapView 升级为 map-hero（深海绿渐变），两页均通过 Playwright 截图复查；前端构建回归通过（12.63s）。

2026-04-24: 完成登录后主干业务页第三轮门户化收口，覆盖合同预警与巡检管理两页。`pms-web/src/views/contract/ContractAlertView.vue` 已从旧 `page-head` 升级为合同风险门户 hero，新增风险总量/高风险项目/严重风险/覆盖范围信号摘要和快捷筛选动作；`pms-web/src/views/inspection/InspectionPlanView.vue` 已升级为巡检门户首屏，新增计划与结果双视图引导、巡检信号摘要和高频动作入口，同时保留原有计划动作流、结果筛选与详情链路。验证：登录态 Playwright 截图复查 `http://localhost:5173/contract/alerts` 与 `http://localhost:5173/inspection/plan`，页面均正常渲染；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误。

2026-04-23: 完成登录后高频入口页第二轮门户化收口，范围覆盖 KPI、统一预警中心、工时报表、项目台账 4 个主干页面。`pms-web/src/views/dashboard/KpiDashboardView.vue` 已从旧 `page-head` 升级为绩效总览 hero，补齐 KPI 信号摘要与快捷入口；`pms-web/src/views/alert/AlertCenterView.vue` 已切到风险门户首屏，补齐风险总量、严重风险、来源分布和快捷筛选；`pms-web/src/views/report/WorkHoursReportView.vue` 已增加月度工时报表 hero、月度控制卡和高频动作区；`pms-web/src/views/project/ProjectListView.vue` 已增加项目台账 hero、项目风险摘要和快捷筛选入口，让这类高频列表页不再由大筛选和大表格直接占满首屏。验证：使用登录态 Playwright 截图复查 `http://localhost:5173/dashboard/kpi`、`http://localhost:5173/alert/center`、`http://localhost:5173/report/workhours`、`http://localhost:5173/project/list`，页面均正常渲染；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误。

2026-04-23: 完成登录后首页与应用壳首轮门户化收口。`pms-web/src/layout/AppLayout.vue` 已补齐侧栏企业概览块，以及内容顶区的标题、角色标签和工作区说明文案，让登录后页面不再只剩平直的导航壳；`pms-web/src/views/dashboard/DashboardView.vue` 已将首页首屏重做为蓝绿企业门户 hero，新增欢迎标题、管理视角标签、4 组状态信号、统计窗口控制卡和快捷入口网格，并保留原有指标卡、图表与下钻逻辑，仅把首屏组织方式改成更明确的工作台。验证：使用登录态 Playwright 截图复查 `http://localhost:5173/dashboard` 与 `http://localhost:5173/dashboard/kpi`，页面均正常渲染且应用壳风格已统一；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误。

2026-04-23: 完成登录页对齐收口。继续在 `pms-web/src/views/login/LoginView.vue` 内对 welcome/login 入口做局部像素对齐调整：右侧登录区增加单一 `access-panel-inner` 内容轴，收紧整体双栏比例与右侧面板宽度，统一标题、Tabs、表单项、验证码块、提示条与底部脚注的左边界和垂直节奏，并同步优化中小屏下的容器宽度回退，消除先前“右侧内容偏散、视觉锚点不一致”的问题。验证：使用 Playwright 截图命令在等待 `.login-shell` 渲染后复查本地 `http://localhost:5173/login`，页面正常渲染且对齐收口生效；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误。

2026-04-23: 完成登录页 welcome 风格重构。基于 `https://pms.bjgoodwill.com/welcome` 的公开登录页结构，在 `pms-web/src/views/login/LoginView.vue` 中将原单一品牌展示页重做为双栏企业门户入口：左侧为蓝绿品牌区、能力卡片和摘要指标，右侧为密码登录/扫码登录双模式切换；密码模式补齐机构代码选择、本地验证码生成与刷新、验证码校验和统一企业登录提示，扫码模式补齐二维码样式、倒计时刷新与切回密码登录交互。认证链路保持不变，最终仍仅调用现有 `/api/auth/login` 的 `account/password`。验证：`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误。

2026-04-23: 完成第二批高频列表页紧凑化收口，重点处理巡检计划、交接管理、合同预警、医院管理四页的关键列不换行与操作区挤压问题。前端方面：`pms-web/src/views/inspection/InspectionPlanView.vue` 已放宽计划表中的组别/巡检人/备注/时效/操作列，以及结果表中的医院/产品/巡检人/风险统计/数据库版本/操作列，并将 `deadline-cell` 与表格动作区改为单行优先；`pms-web/src/views/handover/HandoverListView.vue` 已放宽产品/类型/原组别/提出人/对接人/批次/阶段/邮件日期/操作列，并为详情动作区与看板 `kanban-item-meta` 增加 nowrap/ellipsis；`pms-web/src/views/contract/ContractAlertView.vue` 已放宽医院/组别/销售/合同状态/维护金额/超期天数/操作列，并补齐 fixed 操作区单行约束；`pms-web/src/views/hospital/HospitalListView.vue` 已放宽医院名称/地址/联系人/联系电话/科室数量/评级/操作列，并将评级与操作按钮收口为单行 flex 容器。验证：`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误。

2026-04-23: 完成 AnnualReport workflow 收口，把年度报告从“前端/Upsert 直接改状态、提交日期、评审信息”切到显式 start / submit / complete / reopen 动作流。后端方面：`PMS.Application/Contracts/AnnualReport/IAnnualReportService.cs` 与 `PMS.Infrastructure/Services/InMemoryAnnualReportService.cs` 新增 `GetByIdAsync`、`StartAsync`、`SubmitAsync`、`CompleteAsync`、`ReopenAsync`，并将 create 固定为 `未开始`、update/delete 限制为 `未开始/编写中`、移除 `Status/SubmitDate/Reviewer/ReviewDate` 的直接 upsert；`PMS.API/Controllers/AnnualReport/AnnualReportsController.cs` 新增 workflow PATCH 端点、非法状态 400 返回以及创建/单条动作的医院范围校验；`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` 把 `/api/annual-reports` 权限映射修正为 GET=`annual-report.view`、非 GET=`annual-report.manage`。前端方面：`pms-web/src/api/modules/annual-report.ts` 已接入 workflow 请求，`pms-web/src/views/annual-report/AnnualReportView.vue` 已移除状态、提交日期、评审人、评审日期的内联编辑与新增时直接选状态，改为列表/详情里的“开始编写 / 提交评审 / 完成评审 / 重开”动作按钮，并将 manage 判断从 `annual-report.view` 修正为 `annual-report.manage`。验证：`dotnet build PMS.sln` 通过（0 error，保留既有 CS8625 warnings）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-annual-report-workflow.ps1` 实测 create -> start -> submit -> complete -> reopen -> delete 全链路成功，输出 `createdId=221`、`completeStatus=已完成 reviewer=admin`、`cleanup=deleted`。

2026-04-23: 完成 Handover workflow 收口，把交接管理从“前端按 nextStage 线性推进 + 后端通用 targetStage 更新”切到显式动作流。后端方面：`PMS.Application/Models/Handover/HandoverItemDto.cs` 补齐 `StartedAt`、`CompletedAt`，`PMS.Application/Contracts/Handover/IHandoverService.cs` 与 `PMS.Infrastructure/Services/InMemoryHandoverService.cs` 新增 `SendEmailAsync`、`StartAsync`、`CompleteAsync`、`RollbackAsync`，并修复已交接记录在服务层被移除、无法出现在汇总与列表中的问题；`PMS.API/Controllers/Handover/HandoversController.cs` 新增 `GET /api/handovers/{id}` 和 `PATCH /send-email`、`/start`、`/complete`、`/rollback` 端点，补齐医院范围校验和 `IAuditLogService` 审计；`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` 把 `/api/handovers` 权限映射修正为 GET=`handover.view`、非 GET=`handover.manage`。前端方面：`pms-web/src/api/modules/handover.ts`、`pms-web/src/types/handover.ts` 接入详情接口和显式 workflow 接口，`pms-web/src/views/handover/HandoverListView.vue` 移除 `nextStage/onAdvanceStage` 线性推进逻辑，改为“发交接邮件 / 开始交接 / 完成交接 / 回退一步(重开交接)”动作按钮，并在详情中展示当前责任节点、邮件发送时间、开始交接时间、完成时间。验证：`PMS: Build Frontend` 通过；`dotnet build PMS.sln` 在停掉开发服务后通过（0 warning 0 error）；`powershell -ExecutionPolicy Bypass -File scripts/verify-handover-workflow.ps1` 在现有 `selectedId=2`、`originalStage=已交接` 记录上实测 `rollbackStage=交接中`、`completeStage=已交接`、`finalStage=已交接`，确认动作流可执行且能恢复原始阶段。

2026-04-23: 完成 WorkHours workflow 收口，把工时记录从“只有 CRUD，没有审核动作”补齐为 submit / confirm / reject 流程。后端方面：`PMS.Infrastructure/Services/InMemoryWorkHoursService.cs` 将编辑限制为 `draft/rejected`，允许 `rejected -> submitted` 重提，阻止 `submitted` 记录直接删除；`PMS.API/Controllers/WorkHours/WorkHoursController.cs` 为 `GET /api/workhours/{id}` 与 `PATCH /submit`、`/confirm`、`/reject` 补齐医院范围校验，并让动作端点直接返回最新记录。前端方面：`pms-web/src/api/modules/workhours.ts`、`pms-web/src/types/workhours.ts` 接入 workflow 请求与 `status/confirmedBy/confirmedAt` 字段，`pms-web/src/views/workhours/WorkHoursView.vue` 新增状态列、提交/确认/退回按钮、详情抽屉中的审批信息与动作按钮，并把编辑入口限制到草稿/已退回记录。验证：`dotnet build PMS.sln` 通过（停掉 dev 服务后 0 warning 0 error）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-workhours-workflow.ps1` 实测 create -> submit -> reject -> update -> submit -> confirm -> delete 全链路成功，最终 `detailStatus=confirmed` 且 `cleanup=deleted`。

2026-04-23: 完成 MonthlyReport workflow 收口，把月报从“表单直接改状态”正式切到 submit / approve / reject 动作流。后端方面：`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` 为 `/api/monthly-reports` 补齐权限映射，GET 使用 `monthly-report.view`、非 GET 使用 `monthly-report.manage`；`PMS.Infrastructure/Services/InMemoryMonthlyReportService.cs` 改为 create 固定落草稿、update 仅允许 `draft/rejected` 编辑，状态流转只允许 `SubmitAsync`、`ApproveAsync`、`RejectAsync` 修改，并在提交/通过/驳回时正确维护 `ApprovedBy`、`ApprovedAt`、`RejectionReason`；`PMS.API/Controllers/MonthlyReport/MonthlyReportsController.cs` 对非法编辑态返回 400，避免继续通过 upsert 绕过审批动作。前端方面：`pms-web/src/api/modules/monthly-report.ts`、`pms-web/src/types/monthly-report.ts` 接入 submit/approve/reject 接口与审批字段，`pms-web/src/utils/statusTag.ts` 补齐 rejected 标签，`pms-web/src/views/monthly-report/MonthlyReportView.vue` 移除表单内直接状态选择，新增列表和弹窗内的提交/通过/驳回动作按钮、流程信息面板、审核人/审核时间/驳回原因展示，并限制删除和编辑只对草稿/已驳回记录开放。验证：`dotnet build PMS.sln` 通过（0 error，保留既有 nullable warnings）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-monthly-report-workflow.ps1` 实测 create -> submit -> reject -> update -> submit -> approve -> delete 全链路成功，最终状态为 approved 且 cleanup=deleted。

2026-04-23: 完成 Phase 2 第二轮 workflow 升级，覆盖重大需求与巡检计划两条业务链。后端方面：`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` 修正 `/api/inspections` 的权限映射为 GET=`inspection.view`、非 GET=`inspection.manage`；`PMS.Infrastructure/Services/InMemoryMajorDemandStore.cs` 与 `PMS.API/Controllers/MajorDemand/MajorDemandsController.cs` 新增 `AcceptedAt`、`CompletedAt` 和单条 `accept/complete/reopen` 动作；`PMS.Application/Models/Inspection/InspectionPlanItemDto.cs`、`PMS.Application/Contracts/Inspection/IInspectionService.cs`、`PMS.Infrastructure/Services/InMemoryInspectionService.cs`、`PMS.API/Controllers/Inspection/InspectionsController.cs` 为巡检计划补齐 `StartedAt`、`CompletedAt`、`SlaDueAt`、`start/complete/reopen` 动作和审计日志，并顺手修复 `BuildSeed` 在无 override 时提前返回导致 custom row 新建后“能删不能查”的老问题。前端方面：`pms-web/src/views/major-demand/MajorDemandView.vue` 新增受理/完成/重开按钮、截止时间倒计时标签、详情时间字段，并把 manage 权限判断从“必须 manager”修正为按权限键判断；`pms-web/src/views/inspection/InspectionPlanView.vue` 新增开始执行/完成巡检/重开计划按钮、SLA 截止标签、详情时间字段，并把按钮显隐从 `inspection.view` 改为 `inspection.manage`；`pms-web/src/api/modules/majorDemand.ts`、`pms-web/src/api/modules/inspection.ts`、`pms-web/src/types/inspection.ts` 同步补齐请求与字段。验证：`dotnet build PMS.sln` 通过（0 error 0 warning）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-major-demand-inspection-workflows.ps1` 实测重大需求 add row -> accept -> complete -> reopen -> delete 与巡检 create -> start -> complete -> reopen -> delete 全链路成功，其中 inspection 审计日志命中 3 条对应动作记录。

2026-04-23: 完成 Repair SLA v1 首轮落地，正式把报修工单从“手改状态”切到“动作流转”。后端方面：在 `PMS.Domain/Entities/RepairRecordEntity.cs`、`PMS.Application/Models/RepairRecord/RepairRecordModels.cs` 中新增 `AcceptedAt`、`SlaDueAt` 字段与动作请求 DTO；在 `PMS.Infrastructure/Services/InMemoryRepairRecordService.cs` 中加入轻量 SLA 计算（4h/12h/24h 档位）以及 `AcceptAsync`、`ResolveAsync`、`ReopenAsync`；在 `PMS.API/Controllers/RepairRecord/RepairRecordsController.cs` 暴露 `/accept`、`/resolve`、`/reopen` 端点并写入 `IAuditLogService`。前端方面：`pms-web/src/views/repair/RepairRecordView.vue` 新增 SLA 列与签收/完成/重开按钮，移除新增/编辑表单中的直接改状态入口，并在详情抽屉新增处理人、受理时间、SLA 截止、完成时间和处理节点时间线；`pms-web/src/api/modules/repair.ts` 与 `pms-web/src/types/repair.ts` 同步补齐新接口和字段。验证：`dotnet build PMS.sln` 通过（0 error）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-repair-workflow.ps1` 实测创建临时报修 -> 签收 -> 完成 -> 重开 -> 删除全链路成功，且 `api/audit-logs?module=repair` 命中 3 条对应动作日志。

2026-04-23: 完成全盘审查后第一轮 P0 后端安全加固。在 `PMS.Infrastructure/Services/InMemoryAccessControlService.cs` 把 `/api/system` 通配放行收窄为仅 `/api/system/info`，并为 `/api/system/backup` 强制 `maintenance.manage`（此前任何已登录用户都能直接下载整库快照）；在 `PMS.API/Controllers/Admin/DataImportController.cs` 新增 `TryResolveAllowedFilePath` 白名单解析，把 `text-file`、`major-demand`、`project-ledger` 三个端点的 `FilePath` 限制在 `AppContext.BaseDirectory/Data` 目录内（先前允许任意绝对路径与 `..` 遍历读取本机文件）；在 `PMS.API/Middleware/AuthMiddleware.cs` 与 `PMS.API/Middleware/PermissionMiddleware.cs` 把 `/api/auth/login`、`/api/health` 的公开前缀匹配改为精确匹配 HashSet，避免 `/api/healthxxx` 等前缀绕过；新增 `scripts/verify-security-fixes.ps1` 作为回归脚本。验证：`dotnet build PMS.sln` 通过 0 error 0 warning（本轮改动范围内）；admin `GET /api/system/backup/download` 返回 200 且下载到约 1.6MB 快照；`POST /api/admin/import/text-file` 传入 `C:/Windows/win.ini` 返回 400（“仅允许读取应用 Data 目录下的文件，请改用上传接口”），`major-demand` 端点传入 `../../../../etc/hosts` 同样 400；默认 operator/supervisor/regional_manager 角色均不包含 `maintenance.manage` 权限键，代码路径保证非 admin 会被 403 拦截。

2026-04-23: 完成 Hospital360 联动工作台化增强。在 `pms-web/src/views/hospital/Hospital360View.vue` 为当前激活面板新增“进入对应页面”入口，并为项目、告警、工时、报修四张表补齐行级联动按钮：项目页跳转到 `project/list` 并携带医院/产品/组别筛选；告警页跳转到 `alert/center` 并携带来源/等级/标题关键词；工时页跳转到 `workhours/list` 并携带医院/人员/工时类型；报修页跳转到 `repair/list` 并携带医院/报修人/状态，且单条记录可直接以 `action=detail&id=` 打开工单详情。验证：`PMS: Build Frontend` 通过；Playwright 登录态回归 `hospital/360`，运行时 `ISSUES=0`，并实测新增面板级按钮与项目行级按钮成功跳转到目标页面，截图 `regression-hospital360-premium.png` 已刷新。

2026-04-23: 完成 Hospital360 第二轮细化。继续在 `pms-web/src/views/hospital/Hospital360View.vue` 内收口页面信息优先级：页头新增医院级概览摘要，集中展示合同风险、高优告警、累计工时和开放工单；4 个 tab 的说明区升级为“说明 + 关键指标”双栏结构，并分别补齐超期/覆盖产品、严重告警/主要来源、累计工时/最近填报、开放工单/最近报修等业务摘要；同时将项目、告警、工时、报修四张表改为按风险、时间或处理优先级排序，提升医院级巡检与排查效率。验证：`PMS: Build Frontend` 通过；Playwright 登录态回归 `hospital/360`，运行时 `ISSUES=0`，截图 `regression-hospital360-premium.png` 已刷新。

2026-04-23: 完成 Hospital360 视图主体收口。将 `pms-web/src/views/hospital/Hospital360View.vue` 从裸 `el-tabs + el-table` 结构升级为统一商务化详情页：页头补齐当前医院与总关联记录提示，`SummaryMetrics` 改为与 tab 联动，主体内容切换到 `AppTableCard` 容器，并为项目/告警/工时/报修 4 个面板补齐记录计数、说明条和标签化状态展示，提升医院级聚合信息的层级与可扫读性。验证：`PMS: Build Frontend` 通过；Playwright 登录态回归 `hospital/360`，运行时 `ISSUES=0`，截图 `regression-hospital360-premium.png` 已刷新。

2026-04-22: 完成列表页顶部统计区的共享化收口。新增 `pms-web/src/components/SummaryMetrics.vue`，并在 `pms-web/src/premium-ui.css` 补齐 6 列摘要卡响应式规则与选中态；将 `AlertCenterView`、`ContractAlertView`、`RepairRecordView`、`WorkHoursView`、`InspectionPlanView`、`AnnualReportView`、`HospitalListView`、`HandoverListView`、`ProductListView`、`PersonnelListView`、`Hospital360View` 统一切换到共享摘要卡语言，移除旧式 `stats-row/stat-card` 视觉孤岛，同时修正合同预警“提醒/全部”摘要卡语义不一致的问题。验证：`PMS: Build Frontend` 通过；Playwright 回归 `contract/alerts`、`repair/list`、`workhours/list`、`inspection/plan`、`annual-report/list`、`hospital/list`、`handover/list`、`product/list`、`permission/manage`、`hospital/360`，运行时 `ISSUES=0`，截图确认统计区、筛选区和表格容器的视觉语言已经统一。

2026-04-22: 完成前端商务化精修的最终一致性收口。将 `pms-web/src/views/dashboard/PersonalWorkbench.vue`、`pms-web/src/views/dashboard/SupervisorWorkbench.vue`、`pms-web/src/views/dashboard/RegionalManagerWorkbench.vue`、`pms-web/src/views/audit/AuditLogView.vue`、`pms-web/src/views/dashboard/KpiDashboardView.vue`、`pms-web/src/views/personnel/PersonnelListView.vue` 统一到共享 `metrics-grid`、`metric-card`、`workbench-grid`、`AppTableCard` 与统一分页/权限分组边界样式；同时在 `pms-web/src/utils/echarts-basic.ts` 注册 `GraphicComponent`，修复 KPI 页面中心文字依赖的 ECharts 运行时缺口。验证：`powershell -ExecutionPolicy Bypass -File c:\Users\Administrator\PMS-Standalone\scripts\build-frontend.ps1` 通过；Playwright 复验 `dashboard/kpi`、`audit/log`、`permission/manage` 以及可访问角色工作台，运行时 `ISSUES=0`；最新截图回归已生成 `regression-kpi-premium.png`、`regression-audit-premium.png`、`regression-personnel-premium.png`、`regression-dashboard-operator.png`、`regression-dashboard-supervisor.png`。

2026-04-22: 完成 PMS 前端“上线交付向”商务化精修。核心动作：在 `pms-web/src/main.ts` 启用独立 `premium-ui.css` 设计系统覆盖层，修复 `pms-web/src/style.css` 末尾历史乱码污染；在 `pms-web/src/layout/AppLayout.vue` 增加面包屑、桌面侧栏折叠持久化、页脚与更克制的顶层辅助栏；统一升级 `pms-web/src/components/ProTable.vue`、`pms-web/src/components/AppTableCard.vue`、`pms-web/src/components/ProDrawer.vue`、`pms-web/src/components/AppFormDialog.vue` 的列表/卡片/表单容器；重构 `pms-web/src/views/dashboard/DashboardView.vue` 的指标卡层级与低饱和 ECharts 配色，并重构 `pms-web/src/views/complex-model/MS010001013001View.vue` 的待办地图页头部、面板和任务密度。验证：前端构建通过且无 CSS 压缩告警；登录态 Playwright 截图回归 `dashboard`、`project/list`、`complex-model/ms010001-013-001`、项目编辑抽屉，运行时 `ISSUES=0`，截图显示标题层级、列表阅读密度、表单留白和图表色板已按商务精致风格收口。

2026-04-22: 继续“举一反三”批量修复操作列重叠。已在多页面把 fixed 操作列统一为 `table-action-group` 布局并补足列宽，覆盖 `InspectionPlanView`、`HandoverListView`、`MonthlyReportGenerateView`、`RepairRecordView`、`ProductListView`、`MonthlyReportView`、`AnnualReportView`、`WorkHoursReportView`。验证：前端构建通过；登录态 Playwright 批量回归 `inspection/plan`、`handover/list`、`report/monthly-generate`、`repair/list`、`product/list`、`monthly-report/list`、`annual-report/list`、`report/workhours`，截图确认固定操作列与相邻列未再发生文本重叠。

2026-04-22: 完成表格“固定操作列重叠/透字”通用修复。针对 Element Plus 新旧 fixed 列实现同时加固：在 `pms-web/src/style.css` 补齐 `.el-table-fixed-column--left/right` 粘性单元格背景、斑马纹/悬浮态背景同步、fixed 容器层级与裁切、操作单元格 flex 排布与按钮间距归一；在 `pms-web/src/views/workhours/WorkHoursView.vue` 与 `pms-web/src/views/project/ProjectListView.vue` 统一接入 `table-action-group`。验证：前端构建通过；登录态 Playwright 回归 `project/list` 与 `workhours/list` 页面，操作列与相邻日期/超期列未再出现重叠。

2026-04-22: 紧急修复“登录不进去”问题。定位到后端登录端点启用 `AuthLogin` 限流策略过严（`15 分钟仅 5 次`），导致正常重试后持续返回 429。已在 `PMS.API/Program.cs` 调整为 `PermitLimit = 20` 且 `Window = 1 分钟`，保留拒绝码 429。验证：后端构建通过；联调服务重启后，`POST /api/auth/login` 使用 `account=admin,password=123456` 在 `5173/api` 代理与 `5111` 直连均返回 `code=200` 和 `accessToken`。

2026-04-22: 收口前端企业化风格改造的运行时与打包细节。修复 `pms-web/src/App.vue` 中 `LoginView` 被 `router/index.ts` 动态导入同时又被静态导入导致的构建 warning，改为登录路由走 `router-view`；对 `pms-web/src/views/personnel/PersonnelListView.vue` 做权限页细节修正：操作列动作组重排、编辑抽屉标签宽度放宽，并取消该超宽表格的 `fixed-right` 操作列以避免实际数据下的列叠压错位；在 `pms-web/src/style.css` 中补充 fixed 列底色/阴影样式，统一表格固定列视觉。验证：`PMS: Build Frontend` 通过；使用 admin 账号获得有效会话后，以 Playwright 实际巡检 `dashboard`、`profile`、`permission/manage` 和编辑抽屉态，截图确认布局稳定，运行时 `ISSUES=0`。

2026-04-22: 完成前端交互细节第二轮收口。补齐 `pms-web/src/components/ProDrawer.vue`、`pms-web/src/components/AppFormDialog.vue`、`pms-web/src/components/AppTableCard.vue` 的统一内层结构与头尾区样式；在 `pms-web/src/style.css` 中统一加载遮罩、消息提示、描述列表、空状态、详情布局等通用交互细节；将 `pms-web/src/views/dashboard/PersonalWorkbench.vue`、`pms-web/src/views/dashboard/SupervisorWorkbench.vue`、`pms-web/src/views/dashboard/RegionalManagerWorkbench.vue` 的待办卡片切换到统一卡片组件并收紧空状态/悬浮反馈；重做 `pms-web/src/views/profile/UserProfileView.vue` 的详情页结构，使其与站内其它页面风格一致。验证：修改文件编辑器诊断均为 0 错误；`PMS: Build Frontend` 再次通过（vite build success）。

2026-04-22: 完成前端整体风格第二轮收口，目标对齐成熟大厂企业产品体验。范围集中在共用视觉层与入口壳层：重塑 `pms-web/src/style.css` 的全局设计令牌（颜色、阴影、圆角、背景、按钮、输入框、卡片、分页、表格、弹窗、Tabs 交互），升级 `pms-web/src/components/ProTable.vue` 的筛选区、工具栏和空状态观感，升级 `pms-web/src/layout/AppLayout.vue` 的顶栏、侧栏、通知面板与主内容区层次，并重做 `pms-web/src/views/login/LoginView.vue` 的双栏品牌入口、按钮与表单材质。验证：`PMS: Build Frontend` 构建通过（vite build success）；Playwright 访问 `http://127.0.0.1:5173/` 截图验证登录页真实渲染，浏览器侧 `ISSUES=0`，页面文案与交互入口正常可见。

2026-04-22: 彻底修复本地 PMS 空白页问题（真正根因 + 回归验证）。真因：仓库以 junction（`C:\Users\Administrator\PMS-Standalone` → `D:\project\PMS-Standalone`）接入，`pms-web/vite.config.ts` 将 `projectRoot` 解析为 realpath（`D:\...`），但 `process.cwd()` 仍为 junction 入口（`C:\...`），两套路径不一致导致 Vite 7.3 依赖优化阶段 `esbuildOutputFromId` 匹配失败，抛出 `Cannot read properties of undefined (reading 'imports')`；此前尝试用 `optimizeDeps.noDiscovery: true` 绕过会让浏览器直接请求未经预构建的 `dayjs/dayjs.min.js` 等 CJS 文件，触发 `does not provide an export named 'default'`，页面渲染为空。修复：在 `pms-web/vite.config.ts` 中 `projectRoot` 解析后立即 `process.chdir(projectRoot)`，强制 cwd 与 realpath 对齐；删除之前错误引入的 `optimizeDeps.noDiscovery/include` 片段，恢复 Vite 默认依赖扫描；保留既有 stale-cache 自愈、`scripts/stop-dev.ps1` 逗号分隔端口兼容、`scripts/start-dev.ps1` EncodedCommand 封装。验证：`node_modules/.vite` 清空后 `npm run dev` 启动成功（Vite ready 371ms 无崩溃）；Playwright headless Chromium 访问 `http://127.0.0.1:5173/` 稳态下 `#app` 内容完整渲染（登录页品牌区+表单+提示），页面错误 0、失败请求 0、控制台仅 Vite HMR 连接消息；`PMS: Start Dev (API + Web)` 任务启动后 `http://127.0.0.1:5173/` 返回 200，`http://127.0.0.1:5111/api/health` 返回 401（后端正常响应鉴权要求）。

2026-04-22: 完成前端视觉升级第一轮。统一全局视觉令牌与基础样式（`pms-web/src/style.css`），优化应用壳（`pms-web/src/layout/AppLayout.vue`）顶部导航渐变层次、侧栏选中态与主内容区背景质感，优化登录页（`pms-web/src/views/login/LoginView.vue`）品牌区块、卡片材质、输入焦点反馈与底部提示文案。验证：执行 `npm run build` 时出现既有构建链问题（vite/rolldown 报 `fileName` 收到绝对路径 `D:/project/PMS-Standalone/pms-web/index.html`），需单独处理构建配置；本次样式改动未涉及该配置。

2026-04-22: 集成 `tanweai/pua` 到 PMS 项目级 AI 工作流。通过 SSH 拉取上游仓库后，新增 `.agents/skills/pua/SKILL.md`（Codex 项目级 skill）、`.agents/prompts/pua.md`（手动触发 prompt）和 `.github/prompts/pua.prompt.md`（VS Code Copilot `/pua` 手动触发）。本次未覆盖 `.github/copilot-instructions.md`，以避免影响既有项目指令。

2026-04-22: 新增 VS Code 任务编排与本地停服能力。新增 `.vscode/tasks.json`（PMS: Start Dev / Stop Dev / Build Backend / Build Frontend / Build Full），补充 `scripts/stop-dev.ps1` + `scripts/stop-dev.cmd`（按端口 5111/5173 与专用开发窗口标题停止进程），并新增 `scripts/build-frontend.ps1`（自动加载 NVM + Node 22.15.0 后执行前端构建，规避 shell 无 npm PATH 问题）。验证：`dotnet build PMS.sln` 通过（0 warning, 0 error），`powershell -ExecutionPolicy Bypass -File .\scripts\build-frontend.ps1` 通过（vite build success），`powershell -ExecutionPolicy Bypass -File .\scripts\stop-dev.ps1` 可执行并在无进程时正确提示。

2026-04-22: 新机器环境接入完成。安装 Git、.NET 8 SDK、NVM + Node 22.15.0，恢复前后端依赖；新增 scripts/start-dev.ps1 与 scripts/start-dev.cmd 作为本地一键启动入口；补充传统 PMS.sln 以绕过 PMS.slnx 在当前 dotnet restore 下的兼容问题。验证：dotnet restore PMS.sln 通过，前端 5173 与后端 5111 本地可达。

2026-04-10: 恢复本机通过 bjgoodwill.fun 的外网访问。执行 `scripts/setup-domain-tunnel.ps1` 刷新 Cloudflare Tunnel DNS 与运行配置，启动本机 API（5111）、Vite 前端（5173）与 `pms-bjgoodwill` 隧道；验证：本地前端 200，本地 `/api/health` 401，外网 `https://bjgoodwill.fun/` 200 登录页，`https://bjgoodwill.fun/api/health` 与 `https://api.bjgoodwill.fun/api/health` 401。

2026-03-07: 巡检计划、年度报告补齐前端编辑态；月报来源页到交接/巡检/年报的跳转补充 action 与更精确的医院/产品上下文，并在目标页支持单条命中自动打开。验证：前端诊断无错，npm run build 通过。
2026-03-07: 权限配置页打开与保存后强制刷新上级主管候选人，确保新设为运维主管的人员能立即被其他人员选择为上级主管。验证：PersonnelListView 诊断无错，pms-web 构建通过。
2026-03-07: 权限配置“上级主管”新增兜底候选（无 supervisor/manager 时允许选择服务角色人员）；月度报告生成“组别”改为优先按运维主管(systemRole=supervisor)筛选，缺失时再回退经理组/全组别。验证：pms-web 执行 npm run build 通过。
2026-03-07: 权限配置页收紧“上级主管”候选：仅保留 systemRole=supervisor 或人员岗位口径包含“服务主管/运维服务主管”，不再出现普通运维。验证：PersonnelListView 诊断无错，pms-web 构建通过。
2026-03-07: 权限管理补齐人员编辑扩展字段（性别/婚姻状况/常住地/家庭省市/岗位/用户状态）并持久化到 sourceColumns；上级主管候选调整为运维主管+运维区域经理+运维经理口径，标签显示岗位/角色。验证：前端诊断无错，pms-web 构建通过；后端编译受运行中 PMS.API 锁文件影响未完成。
2026-03-07: 月度报告生成“组别”补齐运维主管口径：岗位识别新增“运维主管/岗位名称”字段别名，并将岗位命中与系统角色 supervisor 统一为“运维主管”标签展示，避免组别下拉缺失运维主管标识。验证：pms-web 执行 npm run build 通过。
2026-03-07: 月度报告生成“组别”增加兜底展示口径：即使无 supervisor 数据也显示“运维主管：未配置/经理代管”，避免只显示纯组名导致误判未生效；并重启 5173 预览服务。验证：pms-web 执行 npm run build 通过，vite dev 启动成功。
2026-03-07: 人员扩展字段（性别等）保存失败排查并修复：确认根因为后端旧进程未重启导致新 DTO 字段未生效；停止旧 PMS.API 进程后重建并重启服务。验证：dotnet build PMS.slnx 通过，Swagger(5111) 返回 200。
2026-03-08: 修复巡检计划页编辑、详情和双击无反馈问题：将巡检计划详情/编辑弹窗从巡检结果 Tab 内移到页面根层，避免在计划页触发时弹窗挂在隐藏容器下不可见。验证：InspectionPlanView 前端诊断无错。
2026-03-08: 排查“重大需求”菜单偶发显示错字，确认前端未做标题字符串计算或改写；为侧边栏菜单强制指定中文字体、字重并关闭字体合成，降低小字号下中文字形误判。验证：AppLayout 前端诊断无错。
2026-03-08: 继续收口菜单栏名称异常显示问题：为桌面/移动菜单栏容器增加 zh-CN 与 translate=no，并统一菜单栏文本字体与 text-size-adjust，避免菜单名称被浏览器翻译或自动缩放干扰。验证：AppLayout 前端诊断无错。
2026-03-08: 修复重大需求单条状态修改串行污染问题：后端为重大需求导入/存量快照统一强制唯一 RowId，并在服务启动时自动修复既有 RowId 冲突；前端表格补充 row-key 稳定行标识，避免状态渲染和选择复用错位。验证：重大需求相关文件诊断通过，后端构建待验证。
2026-03-08: 修复“点击状态标签有统计但无列表”交互问题：年度报告改为状态归一化比对（消除空格/全角空格差异）并将“商机编号”统一改名“机会号”；同时对重大需求与巡检计划的同类状态交互（筛选/按钮显隐）做归一化加固。验证：pms-web 执行 npm run build 通过。
2026-03-09: 全站审计优化批量实施。后端：新增 GlobalExceptionMiddleware 全局异常兜底；CORS 从 AllowAnyOrigin 收紧为可配 WithOrigins。前端：分页 page-sizes 统一为 [15,30,50,100]（14处）；MajorDemandView 分页 layout 补 total/sizes 并接入 useFilterStatePersist；15 个 el-dialog 补 destroy-on-close；PersonnelListView 操作按钮 plain→link；AppLayout router-view 包裹 fade-slide transition；style.css 新增 6 个 CSS 变量 + 过渡动画类；Login 卡片宽度改 min(420px,90vw) 响应式；新增 constants/tableConfig.ts 集中分页常量。
2026-03-10: Task 1 (P0) — Dashboard + AlertCenter 数据范围修复。DashboardController v2/v3 注入 IAccessControlService，所有聚合数据（events、projects、repairs、workHours、inspections）均按 HospitalScope 过滤；AlertCenterController 同步加固。验证：dotnet build 0 错误。
2026-03-10: Task 2 (P1-a) — 个人工作台。后端新增 GET /api/dashboard/workbench 端点，聚合临期合同(30天)、待巡检、未处理报修、本月工时、项目数。前端新建 PersonalWorkbench.vue（3列待办卡片+4统计指标），DashboardView.vue 根据 isManager() 自动切换管理仪表盘/个人工作台。验证：dotnet build 0 错误，npm run build 通过。
2026-03-10: Task 3 (P1-b) — 通知系统。后端新增 NotificationEntity、INotificationService/InMemoryNotificationService（SQLite 持久化，_nextId 自增模式）、NotificationController（4端点：summary/query/mark-read/mark-all-read）。前端新增 types/notification.ts、api/modules/notification.ts，AppLayout.vue 铃铛图标升级为 el-popover+el-badge 通知面板（60秒轮询、标已读、点击导航）。验证：dotnet build 0 错误，npm run build 通过。
2026-03-10: Task 4 (P2-a) — 数据导入UX升级。DataMaintenanceView 导入流程增加文件预校验、进度条反馈、错误明细展示，提升导入体验。验证：npm run build 通过。
2026-03-10: Task 5 (P2-b) — 缺失模块导出补齐。后端为巡检计划、交接记录、医院、年度报告、工时5个模块新增 GET export 端点（CSV 导出）；前端对应6个视图增加导出按钮与 API 调用。验证：dotnet build 0 错误，npm run build 通过。
2026-03-10: Task 6 (P2-c) — 筛选/搜索增强。AlertCenterView 接入 useFilterStatePersist+useLinkedRealtimeRefresh+useResilientLoad 三合一；ProductListView 接入 useFilterStatePersist；ProjectListView/MajorDemandView 接入 useResilientLoad。验证：npm run build 通过。
2026-03-10: Task 7 (P3-a) — 批量操作扩展。产品管理模块新增批量删除：后端 IProductService.BatchDeleteAsync + InMemoryProductService 实现 + ProductController [HttpPost("batch-delete")]；前端 ProductListView 增加多选+批量删除按钮+确认弹窗。验证：dotnet build 0 错误，npm run build 通过。
2026-03-10: Task 8 (P3-b) — 操作日志。全栈实现：AuditLogEntity + IAuditLogService/InMemoryAuditLogService + AuditLogController（分页查询+汇总）；前端 AuditLogView.vue（统计卡片+筛选表单+表格+分页）；路由/权限/菜单全部注册。验证：dotnet build 0 错误 0 警告，npm run build 通过。
2026-03-10: Task 9 (P3-c) — 移动端深度适配。style.css 新增 ~150 行全局响应式 CSS：pointer:coarse 触控优化（44px 最小触控区、触觉反馈）、768px 弹窗/抽屉/表单自适应、576px 超小屏紧凑模式。验证：npm run build 通过。
2026-03-10: Task 10 (P3-d) — 数据分析深化。DashboardView 接入 v2 API (fetchDashboardV2)，新增"告警月度趋势"堆叠面积图（近6个月严重/警告/提醒三线）和"责任人工作量 TOP12"水平堆叠柱状图；analysis-grid 双列布局+1280px 响应式。验证：npm run build 通过。
2026-03-10: Task 11 (P4-a) — 批量导入校验。DataImportController 新增 ImportBatchValidationResult 详情返回，前端增加导入前预检与错误明细。
2026-03-10: Task 12 (P4-b) — Excel 导出。报修记录、巡检计划、交接记录、工时报表的 CSV 导出升级为 ClosedXML Excel 导出（.xlsx），WorkHoursReportView 导出按钮改用 downloadBlob 调用。验证：dotnet build 0 错误，vue-tsc 0 错误。
2026-03-10: Task 13 (P4-c) — 仪表盘时间范围选择器。DashboardView 新增 el-select（1/3/6/12月），fetchDashboardV2 增加 months 参数，后端按 months 过滤事件/告警数据。验证：vue-tsc 0 错误。
2026-03-10: Task 14 (P4-d) — 个人工作台增强。PersonalWorkbench 新增第4列统计卡片（项目总数）、stat cards 增加点击跳转。
2026-03-10: Task 15 (P5-a) — 医院360°视图。新建 Hospital360View.vue（医院选择器+4统计卡+4 tab 数据面板：项目/告警/工时/报修），路由 /hospital/360 注册。验证：vue-tsc 0 错误，dotnet build 0 错误。
2026-03-10: Task 16 (P5-b) — 用户资料页。新建 UserProfileView.vue（基本信息、修改密码表单、数据范围、权限列表），路由 /profile 注册，AppLayout 头部增加"资料"按钮。
2026-03-10: Task 17 (P5-c) — 系统设置页。后端新建 SystemController（GET /api/system/info 返回版本/OS 等元数据），前端新建 SystemSettingsView.vue（系统信息+快捷入口+前端信息），路由 /settings 注册，/api/system 加入权限跳过列表。
2026-03-10: Task 18 (P5-d) — 备份恢复。SqliteJsonStore 暴露 GetDbPath()+CreateConnection()。新建 BackupController（GET download 用 VACUUM INTO 一致快照+POST restore 校验上传 DB 有效性后替换）。前端 system.ts 增加 downloadBackup/uploadRestore，SystemSettingsView 增加备份下载/恢复上传 UI。
2026-03-10: Task 19 (P5-e) — 打印/PDF。新建 composables/usePrint.ts（打开新窗口+注入样式+隐藏非打印元素+window.print），WorkHoursReportView、MonthlyReportGenerateView、AnnualReportView 三个报表视图均增加"打印"按钮+pageRef。
2026-03-10: Task 20 (P5-f) — UX 加固。5个 Dashboard/Workbench 视图主容器增加 v-loading 指令消除首屏空壳闪烁；MS010001013001View（项目地图看板）新增 mapLoading 状态；Controllers/System 文件夹重命名为 Controllers/Infra 修复 C# System 命名空间冲突。验证：vue-tsc 0 错误，dotnet build 0 错误 17 警告。
2026-03-09: 全面审查收口补丁 — 补齐 AuditLogView 的全局组件规范化：筛选/表格容器迁移到 AppFilterCard/AppTableCard，分页尺寸统一改用 tableConfig 常量（PAGE_SIZES/DEFAULT_PAGE_SIZE），并移除页面局部 .filter-card/.table-card 冗余样式。验证：vue-tsc 0 错误，npm run build 通过。
2026-03-09: 继续收口（规范+性能）— AppFormDialog 增强统一行为（size 预设 + close-on-press-escape 默认开启）；Vite 增加 manualChunks 分包；MS010001013001View 改为懒加载 echarts 与 china.geo.json。结果：complex-model-view 由 ~1891.96kB 降至 ~197.48kB，vue-tsc 0 错误，npm run build 通过；当前主要大包剩余 vendor-element-plus / vendor-echarts / china.geo。
2026-03-09: 继续性能压缩 — 前端切换 Element Plus 按需加载（移除 main.ts 全量 app.use(ElementPlus) 与全量样式），改为 Vite unplugin-auto-import + unplugin-vue-components + ElementPlusResolver；MS010001013001View 的 china.geo.json 从 JS 动态导入改为 URL fetch 静态资源加载，避免生成大型 JS 数据块。验证：待本轮构建结果确认。
2026-03-09: 性能压缩结果确认 — 构建通过（vue-tsc 0 错误）；Element Plus 体积显著下降（历史 ~806kB → ~140kB 后再由默认分包拆散），complex-model 业务块降至约 10~13kB 级；china.geo 从 JS chunk 变为静态 json 资源。当前仅剩 ECharts 相关 chunk 约 645kB 超出 500k 警告阈值，属于图表库体积告警。
2026-03-09: ECharts 终轮拆分 — 新增 `utils/echarts-basic.ts` 与 `utils/echarts-map.ts`，Dashboard/KPI 改用 basic，项目地图改用 map；移除旧 `utils/echarts.ts`。构建结果：ECharts 拆为 `echarts-basic`(~96.93kB)、`echarts-map`(~87.05kB) 与 `installCanvasRenderer`(~465.33kB)，不再出现 >500k chunk 警告；vue-tsc 0 错误，npm run build 通过。
2026-04-22: 项目全局UI交互优化升级（处理标题、列间距、空文本按钮等）。通过编写 scripts/add_icons.js 对全局 views 中的 el-button 注入了语义匹配图标（新增/删除/编辑/查看等），并在 main.ts 进行了图标全量注册；同时在 src/style.css 末尾注入了表格头对齐、列 padding 间距、标题 .page-title 伪元素样式，以及 el-button 全局渐变和悬浮动效。验证：前端构建通过，各关键业务页（ProjectList、InspectionPlan等）表头排版和按钮图标反馈符合预期。

2026-04-22: PMS全站大厂级UX/UI体验翻新。引入一套全新的系统化设计语言（参考字节跳动Arco/阿里AntD风格）：通过 pms-web/src/premium-ui.css 收紧全局组件圆角（12px -> 8px），卡片替换为无边线轻质拟真投影（box-shadow: rgba(0,0,0,0.02)），输入框/级联/选择器默认呈现静谧灰（f2f3f5）聚焦微感高亮，表格全面去除分割线保留淡色斑马底及高级浅灰表头（f7f8fa/500字重），按钮体系采用平滑渐变、柔性触感下沉，弹窗/抽屉与分页也同步强化了留白呼吸感。验证：执行 vite build 正常通过。
2026-04-22: 全量升级：极简商务高级风 (Minimalist Business Premium)。按照大型 B 端项目的视觉规范对 PMS 进行了全局降噪和结构优化。
1. 布局重构：去除了顶部和侧边的所有渐变色、复杂阴影、光晕装饰。顶栏统一纯白，左侧菜单平铺纯白并扁平化选中态，用 #f0f2f5 作为全局背景色拉开空间层级。
2. 表格/表单优化：重写 .el-form--inline 规整筛选栏间距；增加 .el-table 极简灰度斑马线并放大行高至 14px 16px；分离分页组件的视觉留白（margin-top: 24px）。
3. 看板/抽屉：缩小了 .stat-card 浮夸的数字大小至 28px，放大模块间距（24px），拉宽 .el-drawer__body 的呼吸区，使得详情及审批界面大幅清爽。
验证：执行 ite build 正常通过，前端核心全局样式注入稳定成功。
