# Progress

2026-06-18: 恢复 `memory-bank/progress.md` 中删除前的历史内容，并对明显 mojibake 行做行级编码逆转。说明：历史记录没有再用摘要替代；少量已经不可逆损坏的字符会保留原样，方便继续从 Git 历史追溯。
2026-06-18: 完成 Ponytail 项目级与 Codex 全局配置。`AGENTS.md` 已增加 `Ponytail Mode`，`C:\Users\Administrator\.codex\AGENTS.md` 已写入全局默认规则，`.agents/skills/ponytail/SKILL.md` 已从上游 `DietrichGebert/ponytail` 接入。验证：确认 `.tmp/ponytail/.codex-plugin/plugin.json`、项目 skill、项目规则和全局规则文件均存在；本机 Codex 插件缓存中已有 `browser`、`chrome`、`github`。


2026-06-01: 已完成当�?PMS 工作区有效改动的 Git 提交与远端对齐。基�?`main` 分支确认本地�?`origin/main` 起始一致后，排除了 `.tmp/` 截图�?`backups/` 本地备份，仅提交源码、数据库�?`memory-bank` 记录；已创建提交 `19c4e29`（`feat: sync project ledger and streamline PMS workflows`）并成功推送到 `origin/main`，当前本�?`main` 与远端已对齐。说明：本次提交使用当前机器默认 Git 身份 `Administrator@liutingDELL13.bjgoodwill.com`；未纳入仓库的仍是本地运行与备份产物�?
2026-06-01: 修复 PMS 本地预览登录超时问题。根因是 `InMemoryAuthService.EnsureAccountSeedsAsync` 在每次登录前对所有已有账号执�?`VerifyPassword(DefaultPassword, ...)`，每个账号触�?100000 �?PBKDF2，导�?`admin/123456` 登录接口耗时�?28 秒，超过前端 authClient 10 秒超时。已将已有账号和受保�?admin 账号的种子逻辑改为仅在密码 hash/salt 缺失时初始化默认密码，不再在登录热路径反复校验并重置默认密码，保�?`/api/auth/login` 契约不变。验证：`dotnet build PMS.sln` 通过（保留既�?26 �?nullable warning）；`dotnet build PMS.slnx` 仍因当前 SDK 不识�?`<Solution>` 失败；`cd pms-web && npm run build` 通过（保留既�?593KB chunk warning）；修复�?`POST /api/auth/login` 使用 `admin/123456` 首次耗时 2.43 秒、再次耗时 1.00 秒并返回 token；Chrome 页面登录�?`http://localhost:5173/login` 成功进入 `http://localhost:5173/dashboard`；端�?`5111` �?`5173` 均保持监听�?
2026-06-01: 已启�?PMS 本地前后端预览服务，未修改业务代码、API 契约或配置默认值，未执行数据导入、同步、备份恢复等会主动改变业务数据的操作。后端通过 `dotnet run --project PMS.API.csproj --launch-profile http` 运行�?`http://localhost:5111`，前端通过 `npm run dev` 运行�?`http://localhost:5173`，日志输出到 `.tmp/pms-api-20260601-100430.*.log` �?`.tmp/pms-web-20260601-100430.*.log`。验证：`http://127.0.0.1:5173/` 返回 200，`http://127.0.0.1:5111/swagger/index.html` 返回 200，`http://127.0.0.1:5173/api/health` 返回 401 Unauthorized（鉴权拦截，确认前端代理和后端链路可达），端�?`5111` �?`5173` 均处于监听状态；已打开浏览器预览页�?Swagger 页�?2026-05-27: 完成 KDocs/WPS 共享文档项目台账增量同步。后端新�?`PMS.Infrastructure/Services/InMemoryProjectDataStore.cs` �?`SyncProjectLedgerFromSource` 合并方法，按 `机会�?医院+产品` 优先匹配、非歧义 `医院+产品` 兜底匹配，保留现有项�?`Id`，只更新项目台账字段并避免清空工�?实施等非台账字段；`PMS.API/Controllers/Admin/DataImportController.cs` 新增 `POST /api/admin/import/upload/sync-project-ledger` 上传入口，复用现�?Excel 解析，默�?`includeGroupName=舒高成`，同步后重建医院派生数据。执行前通过 `/api/system/backup/download` 生成备份 `backups/pms-backup-before-kdocs-project-ledger-sync-20260527-145220.db`；用 `C:\Users\Administrator\WPSDrive\368423903\WPS云盘\项目明细.xlsx` 同步成功：源�?222 行，匹配 215 个，更新 122 个，新增 7 个，保留未命中现有项�?5 个，最终项目总数 227。关联验证：项目总数 227，医院总数 101，合同预警总数 108，年度报告总数 198，巡检总数 228，交接总数 16。验证：`dotnet build PMS.sln` 通过；`dotnet build PMS.slnx` 仍因当前 SDK 不识�?`<Solution>` 失败；`cd pms-web && npm run build` 通过，保留既�?593KB chunk warning�?
2026-05-27: 完成各模块顶部工作台/hero 专业化样式收口。仅修改最终覆盖层 `pms-web/src/clean-ui.css`：为 dashboard/project/contract/alert/inspection/handover/map/kpi/report 顶部工作台增加模块色标、浅色工作台背景、顶部强调线、指标卡左侧色标、右侧动作分栏、control 卡与 quick action 卡统一样式，补齐此前遗漏的 project/inspection/contract/alert/handover/map/kpi/report quick action �?control 覆盖，解决巡检页白底上白字、右侧动作区不易区分的问题；同时给仍使用 `.page-head` 的普通模块增�?4px 主色左边界和浅色工作台背景。验证：`cd pms-web && npm run build` 通过；`dotnet build PMS.slnx` 仍因当前 SDK 不识�?`.slnx` 失败，`dotnet build PMS.sln` 通过；Playwright headless 登录后访�?`http://127.0.0.1:5174/inspection/plan` 确认巡检工作台边界、右侧动作区和快捷动作文字对比度生效，访�?`/product/list` 确认普�?page-head 顶部边界生效�?
2026-05-27: 完成 Dashboard 重大需求空白字段修复并举一反三收口字段解析。新�?`pms-web/src/utils/majorDemandFields.ts`，集中处理重大需求原始导入列�?`_RowId`、需求标题、医�?客户、负责人、计划完成、状态和占位值过滤；`pms-web/src/views/dashboard/DashboardView.vue` 改为�?`_RowId` 关联原始行，重大需�?drill 不再因为只查 `行ID/rowId/ID` 而拿不到医院和负责人；`pms-web/src/views/major-demand/MajorDemandView.vue` 的负责人筛选、列表展示和详情定位同步使用共用解析逻辑，workflow 为空时从 `服务人员` 等原始列兜底。验证：`cd pms-web && npm run build` 通过；`dotnet build PMS.slnx` 因当�?SDK 不识�?`.slnx` 格式失败，`dotnet build PMS.sln` 通过；Playwright headless 登录后访�?`http://127.0.0.1:5174/dashboard` �?`/major-demand/list`，确认重大需求显�?`武汉儿童医院`、`舒高成`、`传染病上报`，列表负责人显示 `舒高�?孟卓/林峰` 等真实值�?
2026-05-27: 完成 PMS 全站分页 UI 统一收口。仅修改最终覆盖层 `pms-web/src/clean-ui.css`，统一 `.pager` �?Element Plus `el-pagination` 的容器、总数、每页条数选择器、跳转输入、普通页码、激活页码、上一�?下一页、禁用态与移动端横向滚动表现，去除旧样式中的渐变、上浮、重阴影和不一致圆角。验证：`cd pms-web && npm run build` 通过；Playwright headless 登录 `admin/123456` 后访�?`http://127.0.0.1:5174/project/list`，确认分页规则加载，页码按钮�?32px 高�?px 圆角、白底边框，激活态为 `#2563eb`，总数与页大小选择器风格统一�?
2026-05-27: 完成 PMS 前端跨模块数据联动与专业化视觉收口。新�?`pms-web/src/utils/dataSync.ts`，集中维护数据变更事件、API 写操�?scope 推断、跨模块依赖扩散�?BroadcastChannel 广播；改�?`pms-web/src/composables/useLinkedRealtimeRefresh.ts`，让页面默认监听数据变更事件，定�?聚焦刷新继续由开关控制，并加入节流去重；�?`pms-web/src/api/request.ts` 中对成功 POST/PUT/PATCH/DELETE 自动广播模块变更，解决“改一处其他地方不跟着动”的基础链路问题。视觉方面继续补�?`pms-web/src/clean-ui.css`，把 dashboard/project/contract/alert/inspection/handover/map/kpi/report hero 和信号卡从强渐变、强阴影收口为白底、弱边框、小圆角和低噪音数据卡。验证：`cd pms-web && npm run build` 通过；新开 `http://127.0.0.1:5174/` 返回 200；Playwright headless 登录后访�?`/dashboard`，模�?`workhours` 数据变更，首页自动重�?`/api/projects`、`/api/alerts/center`、`/api/annual-reports/summary`、`/api/dashboard/v2`，并确认 clean UI hero 覆盖生效�?
2026-05-27: 完成 PMS 前端模块 UI 统一收口。新�?`pms-web/src/clean-ui.css` 并在 `main.ts` 最后加载，统一字体、背景、颜色、圆角、弱阴影、按钮、表单、筛选卡、表格、分页、弹窗和抽屉，减少既�?`style.css`/`premium-ui.css` 叠加造成的渐变、强阴影、hover 位移等视觉噪音；同步收敛 `ProTable`、`AppTableCard`、`AppFormDialog`、`ProDrawer` 局部样式，并将 `MajorDemandView` 的详情原�?`el-drawer` 改为统一 `ProDrawer`。验证：`cd pms-web && npm run build` 通过；本�?Vite 返回 200；Playwright headless 登录后进�?`/major-demand/list`，确认页面非空、clean UI 变量生效、表格卡片弱阴影/小圆角生效、详情抽屉由 `.pro-drawer` 渲染�?
2026-05-27: 完成 PMS 前后端代码审查。本次仅追加审查记录，不改业务代码。后端重点检查认证、权限映射、数据范围过滤、报�?审计/交接/巡检/医院/人员�?API；前端重点检查请求封装、路由权限、登录态和构建。发现的高优先级风险包括 `/api/reports` 未进入统一权限映射、改密后会被认证种子逻辑重置为默认密码、部分医院范围为空时列表/汇总回退全量、Handover 看板未过滤、人�?巡检/报表等部分明细或写接口缺少目标记录范围校验。验证：`dotnet build PMS.slnx` 失败（当�?SDK 不识�?`.slnx` 格式）；`dotnet build PMS.sln` 通过；`pms-web` �?`npm run build` 通过但有�?590KB chunk warning�?
2026-04-24(2): 完成登录后主干业务页第四轮门户化收口，覆盖交接管理与待办项目地图两页。HandoverListView 升级�?handover-hero（蓝紫渐变），MapView 升级�?map-hero（深海绿渐变），两页均通过 Playwright 截图复查；前端构建回归通过�?2.63s）�?
2026-04-24: 完成登录后主干业务页第三轮门户化收口，覆盖合同预警与巡检管理两页。`pms-web/src/views/contract/ContractAlertView.vue` 已从�?`page-head` 升级为合同风险门�?hero，新增风险总量/高风险项�?严重风险/覆盖范围信号摘要和快捷筛选动作；`pms-web/src/views/inspection/InspectionPlanView.vue` 已升级为巡检门户首屏，新增计划与结果双视图引导、巡检信号摘要和高频动作入口，同时保留原有计划动作流、结果筛选与详情链路。验证：登录�?Playwright 截图复查 `http://localhost:5173/contract/alerts` �?`http://localhost:5173/inspection/plan`，页面均正常渲染；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误�?

2026-04-23: 完成登录后高频入口页第二轮门户化收口，范围覆�?KPI、统一预警中心、工时报表、项目台�?4 个主干页面。`pms-web/src/views/dashboard/KpiDashboardView.vue` 已从�?`page-head` 升级为绩效总览 hero，补�?KPI 信号摘要与快捷入口；`pms-web/src/views/alert/AlertCenterView.vue` 已切到风险门户首屏，补齐风险总量、严重风险、来源分布和快捷筛选；`pms-web/src/views/report/WorkHoursReportView.vue` 已增加月度工时报�?hero、月度控制卡和高频动作区；`pms-web/src/views/project/ProjectListView.vue` 已增加项目台�?hero、项目风险摘要和快捷筛选入口，让这类高频列表页不再由大筛选和大表格直接占满首屏。验证：使用登录�?Playwright 截图复查 `http://localhost:5173/dashboard/kpi`、`http://localhost:5173/alert/center`、`http://localhost:5173/report/workhours`、`http://localhost:5173/project/list`，页面均正常渲染；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误�?

2026-04-23: 完成登录后首页与应用壳首轮门户化收口。`pms-web/src/layout/AppLayout.vue` 已补齐侧栏企业概览块，以及内容顶区的标题、角色标签和工作区说明文案，让登录后页面不再只剩平直的导航壳；`pms-web/src/views/dashboard/DashboardView.vue` 已将首页首屏重做为蓝绿企业门�?hero，新增欢迎标题、管理视角标签�? 组状态信号、统计窗口控制卡和快捷入口网格，并保留原有指标卡、图表与下钻逻辑，仅把首屏组织方式改成更明确的工作台。验证：使用登录�?Playwright 截图复查 `http://localhost:5173/dashboard` �?`http://localhost:5173/dashboard/kpi`，页面均正常渲染且应用壳风格已统一；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误�?

2026-04-23: 完成登录页对齐收口。继续在 `pms-web/src/views/login/LoginView.vue` 内对 welcome/login 入口做局部像素对齐调整：右侧登录区增加单一 `access-panel-inner` 内容轴，收紧整体双栏比例与右侧面板宽度，统一标题、Tabs、表单项、验证码块、提示条与底部脚注的左边界和垂直节奏，并同步优化中小屏下的容器宽度回退，消除先前“右侧内容偏散、视觉锚点不一致”的问题。验证：使用 Playwright 截图命令在等�?`.login-shell` 渲染后复查本�?`http://localhost:5173/login`，页面正常渲染且对齐收口生效；`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误�?

2026-04-23: 完成登录�?welcome 风格重构。基�?`https://pms.bjgoodwill.com/welcome` 的公开登录页结构，�?`pms-web/src/views/login/LoginView.vue` 中将原单一品牌展示页重做为双栏企业门户入口：左侧为蓝绿品牌区、能力卡片和摘要指标，右侧为密码登录/扫码登录双模式切换；密码模式补齐机构代码选择、本地验证码生成与刷新、验证码校验和统一企业登录提示，扫码模式补齐二维码样式、倒计时刷新与切回密码登录交互。认证链路保持不变，最终仍仅调用现�?`/api/auth/login` �?`account/password`。验证：`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误�?

2026-04-23: 完成第二批高频列表页紧凑化收口，重点处理巡检计划、交接管理、合同预警、医院管理四页的关键列不换行与操作区挤压问题。前端方面：`pms-web/src/views/inspection/InspectionPlanView.vue` 已放宽计划表中的组别/巡检�?备注/时效/操作列，以及结果表中的医�?产品/巡检�?风险统计/数据库版�?操作列，并将 `deadline-cell` 与表格动作区改为单行优先；`pms-web/src/views/handover/HandoverListView.vue` 已放宽产�?类型/原组�?提出�?对接�?批次/阶段/邮件日期/操作列，并为详情动作区与看板 `kanban-item-meta` 增加 nowrap/ellipsis；`pms-web/src/views/contract/ContractAlertView.vue` 已放宽医�?组别/销�?合同状�?维护金额/超期天数/操作列，并补�?fixed 操作区单行约束；`pms-web/src/views/hospital/HospitalListView.vue` 已放宽医院名�?地址/联系�?联系电话/科室数量/评级/操作列，并将评级与操作按钮收口为单行 flex 容器。验证：`PMS: Build Frontend` 通过，`vue-tsc -b && vite build` 成功完成，未引入新的编译错误�?

2026-04-23: 完成 AnnualReport workflow 收口，把年度报告从“前�?Upsert 直接改状态、提交日期、评审信息”切到显�?start / submit / complete / reopen 动作流。后端方面：`PMS.Application/Contracts/AnnualReport/IAnnualReportService.cs` �?`PMS.Infrastructure/Services/InMemoryAnnualReportService.cs` 新增 `GetByIdAsync`、`StartAsync`、`SubmitAsync`、`CompleteAsync`、`ReopenAsync`，并�?create 固定�?`未开始`、update/delete 限制�?`未开�?编写中`、移�?`Status/SubmitDate/Reviewer/ReviewDate` 的直�?upsert；`PMS.API/Controllers/AnnualReport/AnnualReportsController.cs` 新增 workflow PATCH 端点、非法状�?400 返回以及创建/单条动作的医院范围校验；`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` �?`/api/annual-reports` 权限映射修正�?GET=`annual-report.view`、非 GET=`annual-report.manage`。前端方面：`pms-web/src/api/modules/annual-report.ts` 已接�?workflow 请求，`pms-web/src/views/annual-report/AnnualReportView.vue` 已移除状态、提交日期、评审人、评审日期的内联编辑与新增时直接选状态，改为列表/详情里的“开始编�?/ 提交评审 / 完成评审 / 重开”动作按钮，并将 manage 判断�?`annual-report.view` 修正�?`annual-report.manage`。验证：`dotnet build PMS.sln` 通过�? error，保留既�?CS8625 warnings）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-annual-report-workflow.ps1` 实测 create -> start -> submit -> complete -> reopen -> delete 全链路成功，输出 `createdId=221`、`completeStatus=已完�?reviewer=admin`、`cleanup=deleted`�?

2026-04-23: 完成 Handover workflow 收口，把交接管理从“前端按 nextStage 线性推�?+ 后端通用 targetStage 更新”切到显式动作流。后端方面：`PMS.Application/Models/Handover/HandoverItemDto.cs` 补齐 `StartedAt`、`CompletedAt`，`PMS.Application/Contracts/Handover/IHandoverService.cs` �?`PMS.Infrastructure/Services/InMemoryHandoverService.cs` 新增 `SendEmailAsync`、`StartAsync`、`CompleteAsync`、`RollbackAsync`，并修复已交接记录在服务层被移除、无法出现在汇总与列表中的问题；`PMS.API/Controllers/Handover/HandoversController.cs` 新增 `GET /api/handovers/{id}` �?`PATCH /send-email`、`/start`、`/complete`、`/rollback` 端点，补齐医院范围校验和 `IAuditLogService` 审计；`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` �?`/api/handovers` 权限映射修正�?GET=`handover.view`、非 GET=`handover.manage`。前端方面：`pms-web/src/api/modules/handover.ts`、`pms-web/src/types/handover.ts` 接入详情接口和显�?workflow 接口，`pms-web/src/views/handover/HandoverListView.vue` 移除 `nextStage/onAdvanceStage` 线性推进逻辑，改为“发交接邮件 / 开始交�?/ 完成交接 / 回退一�?重开交接)”动作按钮，并在详情中展示当前责任节点、邮件发送时间、开始交接时间、完成时间。验证：`PMS: Build Frontend` 通过；`dotnet build PMS.sln` 在停掉开发服务后通过�? warning 0 error）；`powershell -ExecutionPolicy Bypass -File scripts/verify-handover-workflow.ps1` 在现�?`selectedId=2`、`originalStage=已交接` 记录上实�?`rollbackStage=交接中`、`completeStage=已交接`、`finalStage=已交接`，确认动作流可执行且能恢复原始阶段�?

2026-04-23: 完成 WorkHours workflow 收口，把工时记录从“只�?CRUD，没有审核动作”补齐为 submit / confirm / reject 流程。后端方面：`PMS.Infrastructure/Services/InMemoryWorkHoursService.cs` 将编辑限制为 `draft/rejected`，允�?`rejected -> submitted` 重提，阻�?`submitted` 记录直接删除；`PMS.API/Controllers/WorkHours/WorkHoursController.cs` �?`GET /api/workhours/{id}` �?`PATCH /submit`、`/confirm`、`/reject` 补齐医院范围校验，并让动作端点直接返回最新记录。前端方面：`pms-web/src/api/modules/workhours.ts`、`pms-web/src/types/workhours.ts` 接入 workflow 请求�?`status/confirmedBy/confirmedAt` 字段，`pms-web/src/views/workhours/WorkHoursView.vue` 新增状态列、提�?确认/退回按钮、详情抽屉中的审批信息与动作按钮，并把编辑入口限制到草稿/已退回记录。验证：`dotnet build PMS.sln` 通过（停�?dev 服务�?0 warning 0 error）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-workhours-workflow.ps1` 实测 create -> submit -> reject -> update -> submit -> confirm -> delete 全链路成功，最�?`detailStatus=confirmed` �?`cleanup=deleted`�?

2026-04-23: 完成 MonthlyReport workflow 收口，把月报从“表单直接改状态”正式切�?submit / approve / reject 动作流。后端方面：`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` �?`/api/monthly-reports` 补齐权限映射，GET 使用 `monthly-report.view`、非 GET 使用 `monthly-report.manage`；`PMS.Infrastructure/Services/InMemoryMonthlyReportService.cs` 改为 create 固定落草稿、update 仅允�?`draft/rejected` 编辑，状态流转只允许 `SubmitAsync`、`ApproveAsync`、`RejectAsync` 修改，并在提�?通过/驳回时正确维�?`ApprovedBy`、`ApprovedAt`、`RejectionReason`；`PMS.API/Controllers/MonthlyReport/MonthlyReportsController.cs` 对非法编辑态返�?400，避免继续通过 upsert 绕过审批动作。前端方面：`pms-web/src/api/modules/monthly-report.ts`、`pms-web/src/types/monthly-report.ts` 接入 submit/approve/reject 接口与审批字段，`pms-web/src/utils/statusTag.ts` 补齐 rejected 标签，`pms-web/src/views/monthly-report/MonthlyReportView.vue` 移除表单内直接状态选择，新增列表和弹窗内的提交/通过/驳回动作按钮、流程信息面板、审核人/审核时间/驳回原因展示，并限制删除和编辑只对草�?已驳回记录开放。验证：`dotnet build PMS.sln` 通过�? error，保留既�?nullable warnings）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-monthly-report-workflow.ps1` 实测 create -> submit -> reject -> update -> submit -> approve -> delete 全链路成功，最终状态为 approved �?cleanup=deleted�?

2026-04-23: 完成 Phase 2 第二�?workflow 升级，覆盖重大需求与巡检计划两条业务链。后端方面：`PMS.Infrastructure/Services/InMemoryAccessControlService.cs` 修正 `/api/inspections` 的权限映射为 GET=`inspection.view`、非 GET=`inspection.manage`；`PMS.Infrastructure/Services/InMemoryMajorDemandStore.cs` �?`PMS.API/Controllers/MajorDemand/MajorDemandsController.cs` 新增 `AcceptedAt`、`CompletedAt` 和单�?`accept/complete/reopen` 动作；`PMS.Application/Models/Inspection/InspectionPlanItemDto.cs`、`PMS.Application/Contracts/Inspection/IInspectionService.cs`、`PMS.Infrastructure/Services/InMemoryInspectionService.cs`、`PMS.API/Controllers/Inspection/InspectionsController.cs` 为巡检计划补齐 `StartedAt`、`CompletedAt`、`SlaDueAt`、`start/complete/reopen` 动作和审计日志，并顺手修�?`BuildSeed` 在无 override 时提前返回导�?custom row 新建后“能删不能查”的老问题。前端方面：`pms-web/src/views/major-demand/MajorDemandView.vue` 新增受理/完成/重开按钮、截止时间倒计时标签、详情时间字段，并把 manage 权限判断从“必�?manager”修正为按权限键判断；`pms-web/src/views/inspection/InspectionPlanView.vue` 新增开始执�?完成巡检/重开计划按钮、SLA 截止标签、详情时间字段，并把按钮显隐�?`inspection.view` 改为 `inspection.manage`；`pms-web/src/api/modules/majorDemand.ts`、`pms-web/src/api/modules/inspection.ts`、`pms-web/src/types/inspection.ts` 同步补齐请求与字段。验证：`dotnet build PMS.sln` 通过�? error 0 warning）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-major-demand-inspection-workflows.ps1` 实测重大需�?add row -> accept -> complete -> reopen -> delete 与巡检 create -> start -> complete -> reopen -> delete 全链路成功，其中 inspection 审计日志命中 3 条对应动作记录�?

2026-04-23: 完成 Repair SLA v1 首轮落地，正式把报修工单从“手改状态”切到“动作流转”。后端方面：�?`PMS.Domain/Entities/RepairRecordEntity.cs`、`PMS.Application/Models/RepairRecord/RepairRecordModels.cs` 中新�?`AcceptedAt`、`SlaDueAt` 字段与动作请�?DTO；在 `PMS.Infrastructure/Services/InMemoryRepairRecordService.cs` 中加入轻�?SLA 计算�?h/12h/24h 档位）以�?`AcceptAsync`、`ResolveAsync`、`ReopenAsync`；在 `PMS.API/Controllers/RepairRecord/RepairRecordsController.cs` 暴露 `/accept`、`/resolve`、`/reopen` 端点并写�?`IAuditLogService`。前端方面：`pms-web/src/views/repair/RepairRecordView.vue` 新增 SLA 列与签收/完成/重开按钮，移除新�?编辑表单中的直接改状态入口，并在详情抽屉新增处理人、受理时间、SLA 截止、完成时间和处理节点时间线；`pms-web/src/api/modules/repair.ts` �?`pms-web/src/types/repair.ts` 同步补齐新接口和字段。验证：`dotnet build PMS.sln` 通过�? error）；`PMS: Build Frontend` 通过；`powershell -ExecutionPolicy Bypass -File scripts/verify-repair-workflow.ps1` 实测创建临时报修 -> 签收 -> 完成 -> 重开 -> 删除全链路成功，�?`api/audit-logs?module=repair` 命中 3 条对应动作日志�?

2026-04-23: 完成全盘审查后第一�?P0 后端安全加固。在 `PMS.Infrastructure/Services/InMemoryAccessControlService.cs` �?`/api/system` 通配放行收窄为仅 `/api/system/info`，并�?`/api/system/backup` 强制 `maintenance.manage`（此前任何已登录用户都能直接下载整库快照）；�?`PMS.API/Controllers/Admin/DataImportController.cs` 新增 `TryResolveAllowedFilePath` 白名单解析，�?`text-file`、`major-demand`、`project-ledger` 三个端点�?`FilePath` 限制�?`AppContext.BaseDirectory/Data` 目录内（先前允许任意绝对路径�?`..` 遍历读取本机文件）；�?`PMS.API/Middleware/AuthMiddleware.cs` �?`PMS.API/Middleware/PermissionMiddleware.cs` �?`/api/auth/login`、`/api/health` 的公开前缀匹配改为精确匹配 HashSet，避�?`/api/healthxxx` 等前缀绕过；新�?`scripts/verify-security-fixes.ps1` 作为回归脚本。验证：`dotnet build PMS.sln` 通过 0 error 0 warning（本轮改动范围内）；admin `GET /api/system/backup/download` 返回 200 且下载到�?1.6MB 快照；`POST /api/admin/import/text-file` 传入 `C:/Windows/win.ini` 返回 400（“仅允许读取应用 Data 目录下的文件，请改用上传接口”），`major-demand` 端点传入 `../../../../etc/hosts` 同样 400；默�?operator/supervisor/regional_manager 角色均不包含 `maintenance.manage` 权限键，代码路径保证�?admin 会被 403 拦截�?

2026-04-23: 完成 Hospital360 联动工作台化增强。在 `pms-web/src/views/hospital/Hospital360View.vue` 为当前激活面板新增“进入对应页面”入口，并为项目、告警、工时、报修四张表补齐行级联动按钮：项目页跳转�?`project/list` 并携带医�?产品/组别筛选；告警页跳转到 `alert/center` 并携带来�?等级/标题关键词；工时页跳转到 `workhours/list` 并携带医�?人员/工时类型；报修页跳转�?`repair/list` 并携带医�?报修�?状态，且单条记录可直接�?`action=detail&id=` 打开工单详情。验证：`PMS: Build Frontend` 通过；Playwright 登录态回�?`hospital/360`，运行时 `ISSUES=0`，并实测新增面板级按钮与项目行级按钮成功跳转到目标页面，截图 `regression-hospital360-premium.png` 已刷新�?

2026-04-23: 完成 Hospital360 第二轮细化。继续在 `pms-web/src/views/hospital/Hospital360View.vue` 内收口页面信息优先级：页头新增医院级概览摘要，集中展示合同风险、高优告警、累计工时和开放工单；4 �?tab 的说明区升级为“说�?+ 关键指标”双栏结构，并分别补齐超�?覆盖产品、严重告�?主要来源、累计工�?最近填报、开放工�?最近报修等业务摘要；同时将项目、告警、工时、报修四张表改为按风险、时间或处理优先级排序，提升医院级巡检与排查效率。验证：`PMS: Build Frontend` 通过；Playwright 登录态回�?`hospital/360`，运行时 `ISSUES=0`，截�?`regression-hospital360-premium.png` 已刷新�?

2026-04-23: 完成 Hospital360 视图主体收口。将 `pms-web/src/views/hospital/Hospital360View.vue` 从裸 `el-tabs + el-table` 结构升级为统一商务化详情页：页头补齐当前医院与总关联记录提示，`SummaryMetrics` 改为�?tab 联动，主体内容切换到 `AppTableCard` 容器，并为项�?告警/工时/报修 4 个面板补齐记录计数、说明条和标签化状态展示，提升医院级聚合信息的层级与可扫读性。验证：`PMS: Build Frontend` 通过；Playwright 登录态回�?`hospital/360`，运行时 `ISSUES=0`，截�?`regression-hospital360-premium.png` 已刷新�?

2026-04-22: 完成列表页顶部统计区的共享化收口。新�?`pms-web/src/components/SummaryMetrics.vue`，并�?`pms-web/src/premium-ui.css` 补齐 6 列摘要卡响应式规则与选中态；�?`AlertCenterView`、`ContractAlertView`、`RepairRecordView`、`WorkHoursView`、`InspectionPlanView`、`AnnualReportView`、`HospitalListView`、`HandoverListView`、`ProductListView`、`PersonnelListView`、`Hospital360View` 统一切换到共享摘要卡语言，移除旧�?`stats-row/stat-card` 视觉孤岛，同时修正合同预警“提�?全部”摘要卡语义不一致的问题。验证：`PMS: Build Frontend` 通过；Playwright 回归 `contract/alerts`、`repair/list`、`workhours/list`、`inspection/plan`、`annual-report/list`、`hospital/list`、`handover/list`、`product/list`、`permission/manage`、`hospital/360`，运行时 `ISSUES=0`，截图确认统计区、筛选区和表格容器的视觉语言已经统一�?

2026-04-22: 完成前端商务化精修的最终一致性收口。将 `pms-web/src/views/dashboard/PersonalWorkbench.vue`、`pms-web/src/views/dashboard/SupervisorWorkbench.vue`、`pms-web/src/views/dashboard/RegionalManagerWorkbench.vue`、`pms-web/src/views/audit/AuditLogView.vue`、`pms-web/src/views/dashboard/KpiDashboardView.vue`、`pms-web/src/views/personnel/PersonnelListView.vue` 统一到共�?`metrics-grid`、`metric-card`、`workbench-grid`、`AppTableCard` 与统一分页/权限分组边界样式；同时在 `pms-web/src/utils/echarts-basic.ts` 注册 `GraphicComponent`，修�?KPI 页面中心文字依赖�?ECharts 运行时缺口。验证：`powershell -ExecutionPolicy Bypass -File c:\Users\Administrator\PMS-Standalone\scripts\build-frontend.ps1` 通过；Playwright 复验 `dashboard/kpi`、`audit/log`、`permission/manage` 以及可访问角色工作台，运行时 `ISSUES=0`；最新截图回归已生成 `regression-kpi-premium.png`、`regression-audit-premium.png`、`regression-personnel-premium.png`、`regression-dashboard-operator.png`、`regression-dashboard-supervisor.png`�?

2026-04-22: 完成 PMS 前端“上线交付向”商务化精修。核心动作：�?`pms-web/src/main.ts` 启用独立 `premium-ui.css` 设计系统覆盖层，修复 `pms-web/src/style.css` 末尾历史乱码污染；在 `pms-web/src/layout/AppLayout.vue` 增加面包屑、桌面侧栏折叠持久化、页脚与更克制的顶层辅助栏；统一升级 `pms-web/src/components/ProTable.vue`、`pms-web/src/components/AppTableCard.vue`、`pms-web/src/components/ProDrawer.vue`、`pms-web/src/components/AppFormDialog.vue` 的列�?卡片/表单容器；重�?`pms-web/src/views/dashboard/DashboardView.vue` 的指标卡层级与低饱和 ECharts 配色，并重构 `pms-web/src/views/complex-model/MS010001013001View.vue` 的待办地图页头部、面板和任务密度。验证：前端构建通过且无 CSS 压缩告警；登录�?Playwright 截图回归 `dashboard`、`project/list`、`complex-model/ms010001-013-001`、项目编辑抽屉，运行�?`ISSUES=0`，截图显示标题层级、列表阅读密度、表单留白和图表色板已按商务精致风格收口�?

2026-04-22: 继续“举一反三”批量修复操作列重叠。已在多页面�?fixed 操作列统一�?`table-action-group` 布局并补足列宽，覆盖 `InspectionPlanView`、`HandoverListView`、`MonthlyReportGenerateView`、`RepairRecordView`、`ProductListView`、`MonthlyReportView`、`AnnualReportView`、`WorkHoursReportView`。验证：前端构建通过；登录�?Playwright 批量回归 `inspection/plan`、`handover/list`、`report/monthly-generate`、`repair/list`、`product/list`、`monthly-report/list`、`annual-report/list`、`report/workhours`，截图确认固定操作列与相邻列未再发生文本重叠�?

2026-04-22: 完成表格“固定操作列重叠/透字”通用修复。针�?Element Plus 新旧 fixed 列实现同时加固：�?`pms-web/src/style.css` 补齐 `.el-table-fixed-column--left/right` 粘性单元格背景、斑马纹/悬浮态背景同步、fixed 容器层级与裁切、操作单元格 flex 排布与按钮间距归一；在 `pms-web/src/views/workhours/WorkHoursView.vue` �?`pms-web/src/views/project/ProjectListView.vue` 统一接入 `table-action-group`。验证：前端构建通过；登录�?Playwright 回归 `project/list` �?`workhours/list` 页面，操作列与相邻日�?超期列未再出现重叠�?

2026-04-22: 紧急修复“登录不进去”问题。定位到后端登录端点启用 `AuthLogin` 限流策略过严（`15 分钟�?5 次`），导致正常重试后持续返�?429。已�?`PMS.API/Program.cs` 调整�?`PermitLimit = 20` �?`Window = 1 分钟`，保留拒绝码 429。验证：后端构建通过；联调服务重启后，`POST /api/auth/login` 使用 `account=admin,password=123456` �?`5173/api` 代理�?`5111` 直连均返�?`code=200` �?`accessToken`�?

2026-04-22: 收口前端企业化风格改造的运行时与打包细节。修�?`pms-web/src/App.vue` �?`LoginView` �?`router/index.ts` 动态导入同时又被静态导入导致的构建 warning，改为登录路由走 `router-view`；对 `pms-web/src/views/personnel/PersonnelListView.vue` 做权限页细节修正：操作列动作组重排、编辑抽屉标签宽度放宽，并取消该超宽表格�?`fixed-right` 操作列以避免实际数据下的列叠压错位；�?`pms-web/src/style.css` 中补�?fixed 列底�?阴影样式，统一表格固定列视觉。验证：`PMS: Build Frontend` 通过；使�?admin 账号获得有效会话后，�?Playwright 实际巡检 `dashboard`、`profile`、`permission/manage` 和编辑抽屉态，截图确认布局稳定，运行时 `ISSUES=0`�?

2026-04-22: 完成前端交互细节第二轮收口。补�?`pms-web/src/components/ProDrawer.vue`、`pms-web/src/components/AppFormDialog.vue`、`pms-web/src/components/AppTableCard.vue` 的统一内层结构与头尾区样式；在 `pms-web/src/style.css` 中统一加载遮罩、消息提示、描述列表、空状态、详情布局等通用交互细节；将 `pms-web/src/views/dashboard/PersonalWorkbench.vue`、`pms-web/src/views/dashboard/SupervisorWorkbench.vue`、`pms-web/src/views/dashboard/RegionalManagerWorkbench.vue` 的待办卡片切换到统一卡片组件并收紧空状�?悬浮反馈；重�?`pms-web/src/views/profile/UserProfileView.vue` 的详情页结构，使其与站内其它页面风格一致。验证：修改文件编辑器诊断均�?0 错误；`PMS: Build Frontend` 再次通过（vite build success）�?

2026-04-22: 完成前端整体风格第二轮收口，目标对齐成熟大厂企业产品体验。范围集中在共用视觉层与入口壳层：重�?`pms-web/src/style.css` 的全局设计令牌（颜色、阴影、圆角、背景、按钮、输入框、卡片、分页、表格、弹窗、Tabs 交互），升级 `pms-web/src/components/ProTable.vue` 的筛选区、工具栏和空状态观感，升级 `pms-web/src/layout/AppLayout.vue` 的顶栏、侧栏、通知面板与主内容区层次，并重�?`pms-web/src/views/login/LoginView.vue` 的双栏品牌入口、按钮与表单材质。验证：`PMS: Build Frontend` 构建通过（vite build success）；Playwright 访问 `http://127.0.0.1:5173/` 截图验证登录页真实渲染，浏览器侧 `ISSUES=0`，页面文案与交互入口正常可见�?

2026-04-22: 彻底修复本地 PMS 空白页问题（真正根因 + 回归验证）。真因：仓库�?junction（`C:\Users\Administrator\PMS-Standalone` �?`D:\project\PMS-Standalone`）接入，`pms-web/vite.config.ts` �?`projectRoot` 解析�?realpath（`D:\...`），�?`process.cwd()` 仍为 junction 入口（`C:\...`），两套路径不一致导�?Vite 7.3 依赖优化阶段 `esbuildOutputFromId` 匹配失败，抛�?`Cannot read properties of undefined (reading 'imports')`；此前尝试用 `optimizeDeps.noDiscovery: true` 绕过会让浏览器直接请求未经预构建�?`dayjs/dayjs.min.js` �?CJS 文件，触�?`does not provide an export named 'default'`，页面渲染为空。修复：�?`pms-web/vite.config.ts` �?`projectRoot` 解析后立�?`process.chdir(projectRoot)`，强�?cwd �?realpath 对齐；删除之前错误引入的 `optimizeDeps.noDiscovery/include` 片段，恢�?Vite 默认依赖扫描；保留既�?stale-cache 自愈、`scripts/stop-dev.ps1` 逗号分隔端口兼容、`scripts/start-dev.ps1` EncodedCommand 封装。验证：`node_modules/.vite` 清空�?`npm run dev` 启动成功（Vite ready 371ms 无崩溃）；Playwright headless Chromium 访问 `http://127.0.0.1:5173/` 稳态下 `#app` 内容完整渲染（登录页品牌�?表单+提示），页面错误 0、失败请�?0、控制台�?Vite HMR 连接消息；`PMS: Start Dev (API + Web)` 任务启动�?`http://127.0.0.1:5173/` 返回 200，`http://127.0.0.1:5111/api/health` 返回 401（后端正常响应鉴权要求）�?

2026-04-22: 完成前端视觉升级第一轮。统一全局视觉令牌与基础样式（`pms-web/src/style.css`），优化应用壳（`pms-web/src/layout/AppLayout.vue`）顶部导航渐变层次、侧栏选中态与主内容区背景质感，优化登录页（`pms-web/src/views/login/LoginView.vue`）品牌区块、卡片材质、输入焦点反馈与底部提示文案。验证：执行 `npm run build` 时出现既有构建链问题（vite/rolldown �?`fileName` 收到绝对路径 `D:/project/PMS-Standalone/pms-web/index.html`），需单独处理构建配置；本次样式改动未涉及该配置�?

2026-04-22: 集成 `tanweai/pua` �?PMS 项目�?AI 工作流。通过 SSH 拉取上游仓库后，新增 `.agents/skills/pua/SKILL.md`（Codex 项目�?skill）、`.agents/prompts/pua.md`（手动触�?prompt）和 `.github/prompts/pua.prompt.md`（VS Code Copilot `/pua` 手动触发）。本次未覆盖 `.github/copilot-instructions.md`，以避免影响既有项目指令�?

2026-04-22: 新增 VS Code 任务编排与本地停服能力。新�?`.vscode/tasks.json`（PMS: Start Dev / Stop Dev / Build Backend / Build Frontend / Build Full），补充 `scripts/stop-dev.ps1` + `scripts/stop-dev.cmd`（按端口 5111/5173 与专用开发窗口标题停止进程），并新增 `scripts/build-frontend.ps1`（自动加�?NVM + Node 22.15.0 后执行前端构建，规避 shell �?npm PATH 问题）。验证：`dotnet build PMS.sln` 通过�? warning, 0 error），`powershell -ExecutionPolicy Bypass -File .\scripts\build-frontend.ps1` 通过（vite build success），`powershell -ExecutionPolicy Bypass -File .\scripts\stop-dev.ps1` 可执行并在无进程时正确提示�?

2026-04-22: 新机器环境接入完成。安�?Git�?NET 8 SDK、NVM + Node 22.15.0，恢复前后端依赖；新�?scripts/start-dev.ps1 �?scripts/start-dev.cmd 作为本地一键启动入口；补充传统 PMS.sln 以绕�?PMS.slnx 在当�?dotnet restore 下的兼容问题。验证：dotnet restore PMS.sln 通过，前�?5173 与后�?5111 本地可达�?

2026-04-10: 恢复本机通过 bjgoodwill.fun 的外网访问。执�?`scripts/setup-domain-tunnel.ps1` 刷新 Cloudflare Tunnel DNS 与运行配置，启动本机 API�?111）、Vite 前端�?173）与 `pms-bjgoodwill` 隧道；验证：本地前端 200，本�?`/api/health` 401，外�?`https://bjgoodwill.fun/` 200 登录页，`https://bjgoodwill.fun/api/health` �?`https://api.bjgoodwill.fun/api/health` 401�?

2026-03-07: 巡检计划、年度报告补齐前端编辑态；月报来源页到交接/巡检/年报的跳转补�?action 与更精确的医�?产品上下文，并在目标页支持单条命中自动打开。验证：前端诊断无错，npm run build 通过�?
2026-03-07: 权限配置页打开与保存后强制刷新上级主管候选人，确保新设为运维主管的人员能立即被其他人员选择为上级主管。验证：PersonnelListView 诊断无错，pms-web 构建通过�?
2026-03-07: 权限配置“上级主管”新增兜底候选（�?supervisor/manager 时允许选择服务角色人员）；月度报告生成“组别”改为优先按运维主管(systemRole=supervisor)筛选，缺失时再回退经理�?全组别。验证：pms-web 执行 npm run build 通过�?
2026-03-07: 权限配置页收紧“上级主管”候选：仅保�?systemRole=supervisor 或人员岗位口径包含“服务主�?运维服务主管”，不再出现普通运维。验证：PersonnelListView 诊断无错，pms-web 构建通过�?
2026-03-07: 权限管理补齐人员编辑扩展字段（性别/婚姻状况/常住�?家庭省市/岗位/用户状态）并持久化�?sourceColumns；上级主管候选调整为运维主管+运维区域经理+运维经理口径，标签显示岗�?角色。验证：前端诊断无错，pms-web 构建通过；后端编译受运行�?PMS.API 锁文件影响未完成�?
2026-03-07: 月度报告生成“组别”补齐运维主管口径：岗位识别新增“运维主�?岗位名称”字段别名，并将岗位命中与系统角�?supervisor 统一为“运维主管”标签展示，避免组别下拉缺失运维主管标识。验证：pms-web 执行 npm run build 通过�?
2026-03-07: 月度报告生成“组别”增加兜底展示口径：即使�?supervisor 数据也显示“运维主管：未配�?经理代管”，避免只显示纯组名导致误判未生效；并重�?5173 预览服务。验证：pms-web 执行 npm run build 通过，vite dev 启动成功�?
2026-03-07: 人员扩展字段（性别等）保存失败排查并修复：确认根因为后端旧进程未重启导致新 DTO 字段未生效；停止�?PMS.API 进程后重建并重启服务。验证：dotnet build PMS.slnx 通过，Swagger(5111) 返回 200�?
2026-03-08: 修复巡检计划页编辑、详情和双击无反馈问题：将巡检计划详情/编辑弹窗从巡检结果 Tab 内移到页面根层，避免在计划页触发时弹窗挂在隐藏容器下不可见。验证：InspectionPlanView 前端诊断无错�?
2026-03-08: 排查“重大需求”菜单偶发显示错字，确认前端未做标题字符串计算或改写；为侧边栏菜单强制指定中文字体、字重并关闭字体合成，降低小字号下中文字形误判。验证：AppLayout 前端诊断无错�?
2026-03-08: 继续收口菜单栏名称异常显示问题：为桌�?移动菜单栏容器增�?zh-CN �?translate=no，并统一菜单栏文本字体与 text-size-adjust，避免菜单名称被浏览器翻译或自动缩放干扰。验证：AppLayout 前端诊断无错�?
2026-03-08: 修复重大需求单条状态修改串行污染问题：后端为重大需求导�?存量快照统一强制唯一 RowId，并在服务启动时自动修复既有 RowId 冲突；前端表格补�?row-key 稳定行标识，避免状态渲染和选择复用错位。验证：重大需求相关文件诊断通过，后端构建待验证�?
2026-03-08: 修复“点击状态标签有统计但无列表”交互问题：年度报告改为状态归一化比对（消除空格/全角空格差异）并将“商机编号”统一改名“机会号”；同时对重大需求与巡检计划的同类状态交互（筛�?按钮显隐）做归一化加固。验证：pms-web 执行 npm run build 通过�?
2026-03-09: 全站审计优化批量实施。后端：新增 GlobalExceptionMiddleware 全局异常兜底；CORS �?AllowAnyOrigin 收紧为可�?WithOrigins。前端：分页 page-sizes 统一�?[15,30,50,100]�?4处）；MajorDemandView 分页 layout �?total/sizes 并接�?useFilterStatePersist�?5 �?el-dialog �?destroy-on-close；PersonnelListView 操作按钮 plain→link；AppLayout router-view 包裹 fade-slide transition；style.css 新增 6 �?CSS 变量 + 过渡动画类；Login 卡片宽度�?min(420px,90vw) 响应式；新增 constants/tableConfig.ts 集中分页常量�?
2026-03-10: Task 1 (P0) �?Dashboard + AlertCenter 数据范围修复。DashboardController v2/v3 注入 IAccessControlService，所有聚合数据（events、projects、repairs、workHours、inspections）均�?HospitalScope 过滤；AlertCenterController 同步加固。验证：dotnet build 0 错误�?
2026-03-10: Task 2 (P1-a) �?个人工作台。后端新�?GET /api/dashboard/workbench 端点，聚合临期合�?30�?、待巡检、未处理报修、本月工时、项目数。前端新�?PersonalWorkbench.vue�?列待办卡�?4统计指标），DashboardView.vue 根据 isManager() 自动切换管理仪表�?个人工作台。验证：dotnet build 0 错误，npm run build 通过�?
2026-03-10: Task 3 (P1-b) �?通知系统。后端新�?NotificationEntity、INotificationService/InMemoryNotificationService（SQLite 持久化，_nextId 自增模式）、NotificationController�?端点：summary/query/mark-read/mark-all-read）。前端新�?types/notification.ts、api/modules/notification.ts，AppLayout.vue 铃铛图标升级�?el-popover+el-badge 通知面板�?0秒轮询、标已读、点击导航）。验证：dotnet build 0 错误，npm run build 通过�?
2026-03-10: Task 4 (P2-a) �?数据导入UX升级。DataMaintenanceView 导入流程增加文件预校验、进度条反馈、错误明细展示，提升导入体验。验证：npm run build 通过�?
2026-03-10: Task 5 (P2-b) �?缺失模块导出补齐。后端为巡检计划、交接记录、医院、年度报告、工�?个模块新�?GET export 端点（CSV 导出）；前端对应6个视图增加导出按钮与 API 调用。验证：dotnet build 0 错误，npm run build 通过�?
2026-03-10: Task 6 (P2-c) �?筛�?搜索增强。AlertCenterView 接入 useFilterStatePersist+useLinkedRealtimeRefresh+useResilientLoad 三合一；ProductListView 接入 useFilterStatePersist；ProjectListView/MajorDemandView 接入 useResilientLoad。验证：npm run build 通过�?
2026-03-10: Task 7 (P3-a) �?批量操作扩展。产品管理模块新增批量删除：后端 IProductService.BatchDeleteAsync + InMemoryProductService 实现 + ProductController [HttpPost("batch-delete")]；前�?ProductListView 增加多�?批量删除按钮+确认弹窗。验证：dotnet build 0 错误，npm run build 通过�?
2026-03-10: Task 8 (P3-b) �?操作日志。全栈实现：AuditLogEntity + IAuditLogService/InMemoryAuditLogService + AuditLogController（分页查�?汇总）；前�?AuditLogView.vue（统计卡�?筛选表�?表格+分页）；路由/权限/菜单全部注册。验证：dotnet build 0 错误 0 警告，npm run build 通过�?
2026-03-10: Task 9 (P3-c) �?移动端深度适配。style.css 新增 ~150 行全局响应�?CSS：pointer:coarse 触控优化�?4px 最小触控区、触觉反馈）�?68px 弹窗/抽屉/表单自适应�?76px 超小屏紧凑模式。验证：npm run build 通过�?
2026-03-10: Task 10 (P3-d) �?数据分析深化。DashboardView 接入 v2 API (fetchDashboardV2)，新�?告警月度趋势"堆叠面积图（�?个月严重/警告/提醒三线）和"责任人工作量 TOP12"水平堆叠柱状图；analysis-grid 双列布局+1280px 响应式。验证：npm run build 通过�?
2026-03-10: Task 11 (P4-a) �?批量导入校验。DataImportController 新增 ImportBatchValidationResult 详情返回，前端增加导入前预检与错误明细�?
2026-03-10: Task 12 (P4-b) �?Excel 导出。报修记录、巡检计划、交接记录、工时报表的 CSV 导出升级�?ClosedXML Excel 导出�?xlsx），WorkHoursReportView 导出按钮改用 downloadBlob 调用。验证：dotnet build 0 错误，vue-tsc 0 错误�?
2026-03-10: Task 13 (P4-c) �?仪表盘时间范围选择器。DashboardView 新增 el-select�?/3/6/12月），fetchDashboardV2 增加 months 参数，后端按 months 过滤事件/告警数据。验证：vue-tsc 0 错误�?
2026-03-10: Task 14 (P4-d) �?个人工作台增强。PersonalWorkbench 新增�?列统计卡片（项目总数）、stat cards 增加点击跳转�?
2026-03-10: Task 15 (P5-a) �?医院360°视图。新�?Hospital360View.vue（医院选择�?4统计�?4 tab 数据面板：项�?告警/工时/报修），路由 /hospital/360 注册。验证：vue-tsc 0 错误，dotnet build 0 错误�?
2026-03-10: Task 16 (P5-b) �?用户资料页。新�?UserProfileView.vue（基本信息、修改密码表单、数据范围、权限列表），路�?/profile 注册，AppLayout 头部增加"资料"按钮�?
2026-03-10: Task 17 (P5-c) �?系统设置页。后端新�?SystemController（GET /api/system/info 返回版本/OS 等元数据），前端新建 SystemSettingsView.vue（系统信�?快捷入口+前端信息），路由 /settings 注册�?api/system 加入权限跳过列表�?
2026-03-10: Task 18 (P5-d) �?备份恢复。SqliteJsonStore 暴露 GetDbPath()+CreateConnection()。新�?BackupController（GET download �?VACUUM INTO 一致快�?POST restore 校验上传 DB 有效性后替换）。前�?system.ts 增加 downloadBackup/uploadRestore，SystemSettingsView 增加备份下载/恢复上传 UI�?
2026-03-10: Task 19 (P5-e) �?打印/PDF。新�?composables/usePrint.ts（打开新窗�?注入样式+隐藏非打印元�?window.print），WorkHoursReportView、MonthlyReportGenerateView、AnnualReportView 三个报表视图均增�?打印"按钮+pageRef�?
2026-03-10: Task 20 (P5-f) �?UX 加固�?�?Dashboard/Workbench 视图主容器增�?v-loading 指令消除首屏空壳闪烁；MS010001013001View（项目地图看板）新增 mapLoading 状态；Controllers/System 文件夹重命名�?Controllers/Infra 修复 C# System 命名空间冲突。验证：vue-tsc 0 错误，dotnet build 0 错误 17 警告�?
2026-03-09: 全面审查收口补丁 �?补齐 AuditLogView 的全局组件规范化：筛�?表格容器迁移�?AppFilterCard/AppTableCard，分页尺寸统一改用 tableConfig 常量（PAGE_SIZES/DEFAULT_PAGE_SIZE），并移除页面局�?.filter-card/.table-card 冗余样式。验证：vue-tsc 0 错误，npm run build 通过�?
2026-03-09: 继续收口（规�?性能）�?AppFormDialog 增强统一行为（size 预设 + close-on-press-escape 默认开启）；Vite 增加 manualChunks 分包；MS010001013001View 改为懒加�?echarts �?china.geo.json。结果：complex-model-view �?~1891.96kB 降至 ~197.48kB，vue-tsc 0 错误，npm run build 通过；当前主要大包剩�?vendor-element-plus / vendor-echarts / china.geo�?
2026-03-09: 继续性能压缩 �?前端切换 Element Plus 按需加载（移�?main.ts 全量 app.use(ElementPlus) 与全量样式），改�?Vite unplugin-auto-import + unplugin-vue-components + ElementPlusResolver；MS010001013001View �?china.geo.json �?JS 动态导入改�?URL fetch 静态资源加载，避免生成大型 JS 数据块。验证：待本轮构建结果确认�?
2026-03-09: 性能压缩结果确认 �?构建通过（vue-tsc 0 错误）；Element Plus 体积显著下降（历�?~806kB �?~140kB 后再由默认分包拆散），complex-model 业务块降至约 10~13kB 级；china.geo �?JS chunk 变为静�?json 资源。当前仅�?ECharts 相关 chunk �?645kB 超出 500k 警告阈值，属于图表库体积告警�?
2026-03-09: ECharts 终轮拆分 �?新增 `utils/echarts-basic.ts` �?`utils/echarts-map.ts`，Dashboard/KPI 改用 basic，项目地图改�?map；移除旧 `utils/echarts.ts`。构建结果：ECharts 拆为 `echarts-basic`(~96.93kB)、`echarts-map`(~87.05kB) �?`installCanvasRenderer`(~465.33kB)，不再出�?>500k chunk 警告；vue-tsc 0 错误，npm run build 通过�?
2026-04-22: 项目全局UI交互优化升级（处理标题、列间距、空文本按钮等）。通过编写 scripts/add_icons.js 对全局 views 中的 el-button 注入了语义匹配图标（新增/删除/编辑/查看等），并�?main.ts 进行了图标全量注册；同时�?src/style.css 末尾注入了表格头对齐、列 padding 间距、标�?.page-title 伪元素样式，以及 el-button 全局渐变和悬浮动效。验证：前端构建通过，各关键业务页（ProjectList、InspectionPlan等）表头排版和按钮图标反馈符合预期�?

2026-04-22: PMS全站大厂级UX/UI体验翻新。引入一套全新的系统化设计语言（参考字节跳动Arco/阿里AntD风格）：通过 pms-web/src/premium-ui.css 收紧全局组件圆角�?2px -> 8px），卡片替换为无边线轻质拟真投影（box-shadow: rgba(0,0,0,0.02)），输入�?级联/选择器默认呈现静谧灰（f2f3f5）聚焦微感高亮，表格全面去除分割线保留淡色斑马底及高级浅灰表头（f7f8fa/500字重），按钮体系采用平滑渐变、柔性触感下沉，弹窗/抽屉与分页也同步强化了留白呼吸感。验证：执行 vite build 正常通过�?
2026-04-22: 全量升级：极简商务高级�?(Minimalist Business Premium)。按照大�?B 端项目的视觉规范�?PMS 进行了全局降噪和结构优化�?
1. 布局重构：去除了顶部和侧边的所有渐变色、复杂阴影、光晕装饰。顶栏统一纯白，左侧菜单平铺纯白并扁平化选中态，�?#f0f2f5 作为全局背景色拉开空间层级�?
2. 表格/表单优化：重�?.el-form--inline 规整筛选栏间距；增�?.el-table 极简灰度斑马线并放大行高�?14px 16px；分离分页组件的视觉留白（margin-top: 24px）�?
3. 看板/抽屉：缩小了 .stat-card 浮夸的数字大小至 28px，放大模块间距（24px），拉宽 .el-drawer__body 的呼吸区，使得详情及审批界面大幅清爽�?
验证：执�?ite build 正常通过，前端核心全局样式注入稳定成功�?
2026-06-02: Refined PMS frontend UI into a restrained premium business style. Updated the final global layer `pms-web/src/clean-ui.css` with refreshed design tokens and overrides for app shell surfaces, cards, tables, filters, buttons, inputs, dialogs, drawers, popovers, and notification items. Tuned `pms-web/src/layout/AppLayout.vue` scoped styles for a compact white header, flatter sidebar, cleaner content topbar, and quieter notification list. Tuned `pms-web/src/views/login/LoginView.vue` so the login shell, logo, fields, QR block, and primary/secondary actions match the 8px radius and low-shadow system. Verification: `cd pms-web && npm run build` passed, with the existing Vite chunk-size warning for the main bundle; local services at `http://127.0.0.1:5173/` and `http://127.0.0.1:5111/swagger/index.html` returned 200; in-app browser checked `/login` computed styles (`.login-shell`, logo, and primary button all 8px radius, primary button `rgb(31, 94, 255)`) and logged in with `admin/123456` to `/dashboard`, confirming the app shell and cards render with the new white/8px UI. Backend build was not rerun because this change did not touch backend code and the preview backend remains running.
2026-06-02: Reworked the project ledger page around real PMS business data from `/api/projects`. `ProjectListView.vue` now loads a large filtered overview dataset for business portrait cards, risk metrics, owner/product/status distribution, data-quality counters, priority project ranking, and a detail drawer that expands service responsibility and commercial delivery fields. The table was reshaped around project object, opportunity number, region, owner, contract status, commercial value, after-sales end date, service status, and detail/edit actions. Verification: `cd pms-web && npm run build` passed with the existing Vite main-bundle chunk warning; in-app browser reloaded the current filtered page for 酒泉市第二人民医�?/ 住院电子病历V6 and confirmed 1 project, sales amount 90�? maintenance amount 0, maintainer 董子�? group 舒高�? overdue 46167�? data-quality counters, table row, and the detail drawer fields render correctly.
2026-06-02: Changed the Dashboard TOP workload bar chart to sales-only contract risk grouping. `DashboardController.GetV2` now builds the workload data from contract alert events only and groups missing names as 未分配销�? so handover/inspection operations personnel no longer appear in that TOP chart. `DashboardView.vue` copy and local state were updated to the sales-risk concept while keeping the existing `ownerWorkload` API field compatible. Verification: `dotnet build PMS.API/PMS.API.csproj -o .tmp/dotnet-build-check` passed with existing nullable warnings, normal `dotnet build PMS.sln` was blocked by the running PMS.API preview process locking DLLs, `cd pms-web && npm run build` passed with the existing Vite main-bundle warning, PMS.API preview was restarted on 5111, and `/api/dashboard/v2?months=6` returned sales names such as 潘双龙、汪明霞、孙立杰 plus 未分配销�?instead of the previous operations personnel mix.

2026-06-03: Completed the frontend-only 运维任务聚合 consumer layer. Added `pms-web/src/types/operations.ts` and `pms-web/src/api/modules/operations.ts` for `/api/operations/tasks/summary` and `/api/operations/tasks`, then built `pms-web/src/components/OperationsTaskPanel.vue` as a clean-ui-compatible reusable panel that accepts `summary`/`tasks`/`loading`, renders summary cards plus a task table, and jumps via `relatedPath/relatedQuery`. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only.
# Progress

2026-06-05: Upgraded `pms-web/src/views/hospital/HospitalListView.vue` from a plain master-data list into a hospital service desk. The page now loads a filtered overview dataset alongside the paged table, adds a hero workspace with current-scope signals, summary metrics, quick focus actions, a data-quality queue, and province/tier/rating insight cards, and keeps the existing create/edit/rating/detail/delete flows, route-driven edit deep links, and realtime refresh wiring intact. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only; a headless Playwright check logged in with `admin/123456`, opened `http://127.0.0.1:5173/hospital/list`, confirmed 1 hero block, 4 insight cards, 4 quick actions, and 4 summary metrics, clicked the `未完成评级` quick action to change the active scope badge from `当前全部范围` to `未完成评级`, and captured `D:\project\PMS-Standalone\.tmp\verification\hospital-view.png`.
2026-06-05: Upgraded `pms-web/src/views/product/ProductListView.vue` from a plain master-data list into a product service desk. The page now loads a filtered overview dataset alongside the paged table, adds a hero workspace with current-scope signals, summary metrics, quick focus actions, deployment ranking, category/status insight cards, a basic data-quality queue, and client-side CSV export over the active result set, while keeping the existing create/edit/detail/delete/batch-delete flows, route-driven edit deep links, and realtime refresh wiring intact. Verification: direct `npm run build` in the current restricted shell was blocked by the sandboxed Node runtime resolving `C:\Users\Administrator` with `EPERM`, so this slice was validated through the live Vite dev server instead: fetching `http://127.0.0.1:5173/src/views/product/ProductListView.vue?t=2` returned 200 after the missing import fix, and a headless Playwright check authenticated via `/api/auth/login`, opened `http://127.0.0.1:5173/product/list`, confirmed 1 hero block, 4 insight cards, 4 quick actions, and 4 summary metrics, then clicked the `高部署产品` quick action and confirmed the active scope badge changed from `当前全部范围` to `高部署产品` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/personnel/PersonnelListView.vue` from a plain permission/personnel list into a personnel service desk. The page now adds a hero workspace with current-scope signals, quick focus actions, a data-quality queue, group and supervisor distribution, onsite/role structure cards, and filtered summary metrics while preserving the existing external sync, create/edit/detail, permission configuration, route-driven edit deep links, and linked refresh behavior. Verification: fetching `http://127.0.0.1:5173/src/views/personnel/PersonnelListView.vue?t=1` from the live Vite dev server returned 200, proving the module compiled in the current runtime; a headless Playwright check authenticated via `/api/auth/login`, opened the personnel route and landed on `http://127.0.0.1:5173/permission/manage`, confirmed 1 hero block, 4 insight cards, 4 quick actions, and 4 summary metrics, then clicked the `驻场人员` quick action and confirmed the active scope badge changed from `当前全部范围` to `驻场人员` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/handover/HandoverListView.vue` from a workflow-heavy list page into a clearer handover service desk. The page now derives a filter-scoped overview from the existing full handover dataset, aligns first-screen summary values with the active filter state, and adds four insight cards for the priority queue, source group distribution, owner load, and batch/type structure while preserving the existing export, detail drawer, workflow actions, route deep links, and kanban behavior. Verification: fetching `http://127.0.0.1:5173/src/views/handover/HandoverListView.vue?t=1` from the live Vite dev server returned 200, proving the module compiled in the current runtime; a headless Playwright check authenticated via `/api/auth/login` with `{ account: 'admin', password: '123456' }`, seeded `pms-auth-token`, `pms-auth-session`, and `pms-current-personnel-id` into localStorage, opened `http://127.0.0.1:5173/handover/list`, confirmed 1 hero block, 4 insight cards, and 4 quick actions, then clicked the first quick action and confirmed the active scope badge changed from `全部交接记录` to `阶段：未发` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/major-demand/MajorDemandView.vue` from a plain workflow list into a major-demand service desk. The page now adds a hero workspace, quick actions, summary cards, and four insight cards for the priority queue, hospital distribution, owner load, and status structure, all derived from the current filter-scoped `displayRows` dataset while preserving the existing batch actions, workflow actions, detail drawer, comment flow, edit flow, and export behavior. Verification: fetching `http://127.0.0.1:5173/src/views/major-demand/MajorDemandView.vue?t=1` from the live Vite dev server returned 200, proving the module compiled in the current runtime; a headless Playwright check authenticated via `/api/auth/login` with `{ account: 'admin', password: '123456' }`, seeded `pms-auth-token`, `pms-auth-session`, and `pms-current-personnel-id` into localStorage, opened `http://127.0.0.1:5173/major-demand/list`, confirmed 1 hero block, 4 insight cards, and 4 quick actions, then clicked the first quick action and confirmed the active scope badge changed from `全部重大需求` to `状态：待处理` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/alert/AlertCenterView.vue` from a summary-first list page into an alert command desk. The page now uses `SummaryMetrics` for the six top-level alert cards, loads a filtered overview dataset from the same `/alerts/center` endpoint as the table, and adds four insight cards for the priority queue, hospital concentration, owner load, and source structure while preserving the existing filter, export, pagination, and row jump behavior. Verification: fetching `http://127.0.0.1:5173/src/views/alert/AlertCenterView.vue?t=1` from the live Vite dev server returned 200, proving the module compiled in the current runtime; a headless Playwright check authenticated via `/api/auth/login` with `{ account: 'admin', password: '123456' }`, seeded `pms-auth-token`, `pms-auth-session`, and `pms-current-personnel-id` into localStorage, opened `http://127.0.0.1:5173/alert/center`, confirmed 1 hero block, 4 insight cards, 4 quick actions, and 6 summary cards, then clicked the first quick action and confirmed the active scope badge changed from `全量风险视图` to `级别：严重` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/inspection/InspectionPlanView.vue` from a tabbed workflow page into a clearer inspection command desk. The page now adds four first-screen insight cards to the plan tab (priority queue, hospital concentration, inspector load, type/priority structure) and four to the result tab (priority results, hospital distribution, inspector result load, health-level structure), keeps hero signals and summary metrics aligned with the filtered plan/result scope, and extends `loadResults()` with a filtered full-result fetch so the result-side overview uses the same business口径 as the table and quick actions. Verification: fetching `http://127.0.0.1:5173/src/views/inspection/InspectionPlanView.vue?t=3` from the live Vite dev server returned 200 after fixing the injected string corruption in the new computed state; a headless Playwright check authenticated via `/api/auth/login` with `{ account: 'admin', password: '123456' }`, seeded `pms-auth-token`, `pms-auth-session`, and `pms-current-personnel-id` into localStorage using `data.accessToken`, opened `http://127.0.0.1:5173/inspection/plan`, confirmed 1 hero block, 4 visible insight cards in the plan tab, 4 quick actions, visible summary text for `已计划`, clicked the first quick action and confirmed the active scope badge changed from `巡检计划视图` to `计划：执行中`, switched to the result tab, and confirmed 4 visible result insight cards plus visible summary text for `全部结果` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/contract/ContractAlertView.vue` from a summary-first list page into a clearer contract-risk service desk. The page now loads a filtered full alert dataset alongside the paged table, derives summary metrics and hero signals from that same filtered scope, and adds four insight cards for the priority queue, hospital concentration, service-group load, and validity/level structure while preserving the existing filter form, export flow, and jump-to-project action. Verification: fetching `http://127.0.0.1:5173/src/views/contract/ContractAlertView.vue?t=2` from the live Vite dev server returned 200, proving the module compiled in the current runtime; a headless Playwright check authenticated via `/api/auth/login` with `{ account: 'admin', password: '123456' }`, seeded `pms-auth-token`, `pms-auth-session`, and `pms-current-personnel-id` into localStorage using `data.accessToken`, opened `http://127.0.0.1:5173/contract/alerts`, confirmed 1 hero block, 4 visible insight cards, 4 quick actions, visible summary text for `全部`, then clicked the first quick action and confirmed the active scope badge changed from `全量风险视图` to `级别：严重` with no runtime errors.
2026-06-05: Upgraded `pms-web/src/views/dashboard/DashboardView.vue` from a chart-first manager page into a clearer global command desk. The page now adds a first-screen insight layer above the operations task panel and chart grid, with four cards for the priority task queue, risk-source structure, regional risk focus, and closure pressure, all derived from the same live project, alert, annual-report, major-demand, dashboard-v2, and operations-task datasets already loaded by the page. Verification: fetching `http://127.0.0.1:5173/src/views/dashboard/DashboardView.vue?t=2` from the live Vite dev server returned 200, proving the module compiled in the current runtime; a headless Playwright check authenticated via `/api/auth/login` with `{ account: 'admin', password: '123456' }`, seeded `pms-auth-token`, `pms-auth-session`, and `pms-current-personnel-id` into localStorage using `data.accessToken`, opened `http://127.0.0.1:5173/dashboard`, confirmed 1 hero block, 4 visible insight cards (`重点任务队列` / `风险来源结构` / `区域风险关注` / `闭环压力概览`), and the operations task panel, then clicked the first hero quick action and confirmed navigation from the dashboard to `http://127.0.0.1:5173/alert/center?level=%E4%B8%A5%E9%87%8D` with no runtime errors.

2026-06-04: Upgraded `pms-web/src/views/workhours/WorkHoursView.vue` from a plain list page into a work-hours service desk. The page now loads filtered overview data alongside the paged table, adds a hero workspace with work-hours signals, quick actions, review queue, personnel/hospital/type insight cards, and keeps overview/table state synchronized across search, reset, type filtering, route-query changes, and create/update/delete/workflow actions. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only; a headless Playwright check logged in with `admin/123456`, opened `http://127.0.0.1:5173/workhours/list`, confirmed the hero block, 4 insight cards, 4 quick actions, summary strip, and work-hours table text are visible, and captured `D:\project\PMS-Standalone\.tmp\verification\workhours-view.png`.
2026-06-04: Aligned work-hours summary metrics with the active filter scope. Backend changed `PMS.API/Controllers/WorkHours/WorkHoursController.cs`, `PMS.Application/Contracts/WorkHours/IWorkHoursService.cs`, `PMS.Application/Models/WorkHours/WorkHoursModels.cs`, and `PMS.Infrastructure/Services/InMemoryWorkHoursService.cs` so `/api/workhours/summary` accepts the same personnel/hospital/date/type filters and access scope as `/api/workhours`, and the shared filter logic now covers both list and summary calculations. Frontend changed `pms-web/src/api/modules/workhours.ts` and `pms-web/src/views/workhours/WorkHoursView.vue` so search/reset/type changes reload summary, overview, and table data together. Verification: `dotnet build PMS.API/PMS.API.csproj -o .tmp/dotnet-build-check` passed with existing repository warnings only; `cd pms-web && npm run build` passed with the existing Vite chunk-size warning; authenticated API checks confirmed `/api/workhours/summary?workDateFrom=2099-01-01&workDateTo=2099-01-31` returns `total=0` and `totalHours=0` while the matching list query returns `total=0`; a headless Playwright check logged in with `admin/123456`, opened `http://127.0.0.1:5173/workhours/list?dateFrom=2099-01-01&dateTo=2099-01-31`, confirmed the filter badge and hero signal values are all zero, and captured `D:\project\PMS-Standalone\.tmp\verification\workhours-filtered-summary.png`.
2026-06-04: Completed supervisor integration for the operations-first task hub. Backend now serves `/api/operations/tasks` and `/api/operations/tasks/summary` through `PMS.API/Controllers/Operations/OperationsController.cs`, with `IOperationsTaskService` registered in `PMS.API/Program.cs` and `/api/operations` permission-mapped in `PMS.Infrastructure/Services/InMemoryAccessControlService.cs`. The aggregation service (`PMS.Infrastructure/Services/InMemoryOperationsTaskService.cs`) consolidates contract risk, repairs, inspections, handovers, major demands, annual reports, monthly reports, and project data-quality issues into one operations queue and respects the current hospital scope. Frontend added `pms-web/src/api/modules/operations.ts`, `pms-web/src/types/operations.ts`, `pms-web/src/composables/useOperationsTasks.ts`, and `pms-web/src/components/OperationsTaskPanel.vue`, then wired the panel into `DashboardView.vue`, `PersonalWorkbench.vue`, `SupervisorWorkbench.vue`, and `RegionalManagerWorkbench.vue`. Also fixed the `AppTableCard` header slot passthrough and corrected workbench inspection jumps from `/inspection/list` to `/inspection/plan`. Verification: `dotnet build PMS.sln` passed, `cd pms-web && npm run build` passed, `POST /api/auth/login` with `admin/123456` succeeded, and authenticated calls to `/api/operations/tasks/summary` plus `/api/operations/tasks?page=1&size=3` returned non-empty task data.
2026-06-04: Continued the operations-first rollout by turning the task panel from read-only into an actionable filter surface. `pms-web/src/components/OperationsTaskPanel.vue` now supports quick level filtering from summary cards, source and keyword filters, reset, and refresh; `pms-web/src/composables/useOperationsTasks.ts` now keeps shared query state and exposes `applyQuery` / `resetQuery`; `DashboardView.vue`, `PersonalWorkbench.vue`, `SupervisorWorkbench.vue`, and `RegionalManagerWorkbench.vue` now bind the shared query into the panel so all four entry pages behave consistently. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only.
2026-06-04: Expanded the operations task panel to support supervisor-style triage by owner and hospital. `pms-web/src/components/OperationsTaskPanel.vue` now includes filters for `owner` and `hospitalName`, shows the active filter summary, and keeps the responsive filter layout stable across desktop and mobile widths. The shared query wiring already added in `useOperationsTasks.ts` continues to power manager, supervisor, regional manager, and personal workbench entry pages without backend contract changes. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only.
2026-06-04: Turned `/maintenance/data` into a real landing page for operations data-quality tasks. `pms-web/src/views/maintenance/DataMaintenanceView.vue` now consumes `hospitalName` and `productName` from the route query, shows a task-focus alert, prefills the ownership-adjustment form, adds audit-table filters for hospital/product/group, narrows the table to the focused record set, and lets users click a row or the new `填入调整` action to carry the current record into the reassignment form. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only; browser verification after logging in with `admin/123456` confirmed the focused maintenance page shows the task banner, hospital/product labels, filter controls, one matching audit row, and the `填入调整` action for `酒泉市第二人民医�?/ 住院电子病历V6`.
2026-06-04: Upgraded `pms-web/src/views/repair/RepairRecordView.vue` from a plain list page into a repair service desk. The page now loads a filtered overview dataset in parallel with the paged table, adds a hero work area with repair signals, quick actions, and four insight cards (priority queue, hospital concentration, assignee load, issue categories), and keeps those cards synchronized after search/reset/status filtering, manual refresh, route-query deep links, and create/update/delete/workflow actions. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only; a headless Playwright check logged in with `admin/123456`, opened `http://127.0.0.1:5173/repair/list`, confirmed the hero block, 4 insight cards, 4 quick actions, and summary strip are visible, and captured `D:\project\PMS-Standalone\.tmp\verification\repair-record-view.png`.
2026-06-04: Upgraded `pms-web/src/views/annual-report/AnnualReportView.vue` from a plain summary/list page into an annual-report service desk. The page now derives first-screen signals, quick actions, overdue/review queues, service-person/group/status insight cards, and active-scope labels from the current filtered dataset while preserving the existing inline editing, create dialog, workflow actions, print action, route deep links, and filter persistence. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only; a headless Playwright check logged in with `admin/123456`, opened `http://127.0.0.1:5173/annual-report/list`, confirmed the hero block, 4 insight cards, 4 quick actions, and the `年报明细` table header are visible, and captured `D:\project\PMS-Standalone\.tmp\verification\annual-report-view.png`.
2026-06-05: Upgraded `pms-web/src/views/monthly-report/MonthlyReportView.vue` from a plain filter/list page into a monthly-report service desk. The page now adds a hero workspace, summary cards, quick actions, a review/rejected queue, hospital/submitter/status insight cards, active-scope labels, and table header context derived from a frontend overview dataset loaded with the existing monthly-report APIs. Existing create/edit/submit/approve/reject/delete/dialog/deep-link flows remain intact and now refresh both the overview and the paged table together after mutations. Verification: `cd pms-web && npm run build` passed with the existing Vite chunk-size warning only; a headless Playwright check logged in with `admin/123456`, opened `http://127.0.0.1:5173/monthly-report/list`, confirmed the hero block, 4 insight cards, 4 quick actions, and the `月报明细` table header are visible, and captured `D:\project\PMS-Standalone\.tmp\verification\monthly-report-view.png`.
